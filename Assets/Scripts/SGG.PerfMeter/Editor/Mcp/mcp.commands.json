{
  "schema_version": 1,
  "package": "com.sungeargames.perfmeter",
  "commands": [
    {
      "id": "perfmeter.setup.status",
      "title": "PerfMeter setup status",
      "description": "Read SGG PerfMeter setup status.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.SetupStatus",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "setup"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.compatibility.status",
      "title": "PerfMeter compatibility status",
      "description": "Read separate import, core runtime, and active render integration compatibility with declared version floors and reasons.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.CompatibilityStatus",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "compatibility", "setup"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.setup.run",
      "title": "PerfMeter recommended setup",
      "description": "Run the recommended SGG PerfMeter project setup actions.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.SetupRun",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 60000,
      "tags": ["perfmeter", "setup"],
      "capability": "perfmeter",
      "requires_editor_status": ["editing"],
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.runtime.status",
      "title": "PerfMeter runtime status",
      "description": "Read SGG PerfMeter runtime status.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.RuntimeStatus",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "runtime"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
	{
	  "id": "perfmeter.runtime.ensure",
      "title": "Start PerfMeter runtime",
      "description": "Ensure SGG PerfMeter runtime is running.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.RuntimeEnsure",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "runtime"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.runtime.stop",
      "title": "Stop PerfMeter runtime",
      "description": "Stop SGG PerfMeter runtime if it is running.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.RuntimeStop",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "runtime"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.runtime.reset_stats",
      "title": "Reset PerfMeter runtime stats",
      "description": "Reset SGG PerfMeter rolling runtime stats, alert counters, and active session counters without stopping the runtime.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.RuntimeResetStats",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "runtime", "reset"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.runtime.mode.set",
      "title": "Set PerfMeter collection mode",
      "description": "Set SGG PerfMeter collection mode to Stopped, Background, Overlay, or OverdrawDiagnostic. Overlay visibility requests persist across Play Mode transitions for the current Editor session; status distinguishes requested, active-component, deferred, and unobserved rendered state.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.RuntimeModeSet",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "runtime", "mode"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["Stopped", "Background", "Overlay", "OverdrawDiagnostic"] },
          "frame_count": { "type": "integer", "minimum": 1, "maximum": 600 }
        },
        "required": ["mode"],
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.metrics.latest",
      "title": "PerfMeter latest metrics",
      "description": "Read the latest SGG PerfMeter metrics snapshot, including registered custom metrics.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.MetricsLatest",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "metrics"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.platform.telemetry",
      "title": "PerfMeter platform telemetry",
      "description": "Read optional thermal, temperature trend, CPU/GPU performance level, and adaptive bottleneck telemetry with per-field availability.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.PlatformTelemetry",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "platform", "thermal", "telemetry"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.profiler.capabilities",
      "title": "PerfMeter Profiler metric capabilities",
      "description": "Read the cached ProfilerRecorder metric catalog, exact or alias resolution provenance, and unavailable/no-sample/sampled states without starting the runtime or performing discovery.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.ProfilerCapabilities",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "profiler", "capabilities"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.alerts.latest",
      "title": "PerfMeter latest alerts",
      "description": "Read active SGG PerfMeter alerts and alert counters.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.AlertsLatest",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "alerts"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.alerts.clear",
      "title": "Clear PerfMeter alerts",
      "description": "Clear SGG PerfMeter active alerts, alert counters, and per-rule cooldown state.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.AlertsClear",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "alerts"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.alerts.capture.begin",
      "title": "Begin PerfMeter alert capture scope",
      "description": "Classify alerts fired during a bounded external capture as Capture. End the scope with the same capture_id.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.AlertsCaptureBegin",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "alerts", "capture"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "required": ["capture_id"],
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.alerts.capture.end",
      "title": "End PerfMeter alert capture scope",
      "description": "End the bounded alert capture scope. The capture_id must match the active scope.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.AlertsCaptureEnd",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "alerts", "capture"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "required": ["capture_id"],
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.capture.request",
      "title": "Request PerfMeter external GPU capture",
      "description": "Request one bounded RenderDoc or PIX capture with correlated baseline/capture samples and optional runtime screenshot. Requires Play Mode and an already attached supported external tool.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.CaptureRequest",
      "risk": "write",
      "idempotency": "unsafe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "capture", "bundle"],
      "capability": "perfmeter",
      "requires_editor_status": ["playing"],
      "schema": {
        "type": "object",
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 },
          "tool": { "type": "string", "enum": ["RenderDoc", "Pix"] },
          "capture_frames": { "type": "integer", "minimum": 1, "maximum": 120 },
          "pre_roll_frames": { "type": "integer", "minimum": 0, "maximum": 600 },
          "post_roll_frames": { "type": "integer", "minimum": 0, "maximum": 600 },
          "include_screenshot": { "type": "boolean" }
        },
        "required": ["capture_id", "tool"],
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.capture.status",
      "title": "Read PerfMeter capture status",
      "description": "Read external capture and correlated bundle state without starting the runtime.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.CaptureStatus",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "capture", "bundle"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.capture.cancel",
      "title": "Cancel PerfMeter external GPU capture",
      "description": "Cancel the matching capture and retry owned backend cleanup when necessary.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.CaptureCancel",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "capture"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 }
        },
        "required": ["capture_id"],
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.capture.export",
      "title": "Export PerfMeter capture bundle",
      "description": "Atomically export a completed correlated capture bundle under Temp/PerfMeter/CaptureBundles. Existing destinations are never overwritten.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.CaptureExport",
      "risk": "write",
      "idempotency": "unsafe",
      "timeout_ms": 30000,
      "tags": ["perfmeter", "capture", "bundle", "export"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 },
          "path": { "type": "string" },
          "external_artifact_path": { "type": "string" },
          "require_authoritative_external_artifact": { "type": "boolean" }
        },
        "required": ["capture_id"],
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.capture.capabilities",
      "title": "Read PerfMeter capture capabilities",
      "description": "Read capture tool/platform gates, bundle limits, retention, screenshot support, and external-artifact authority policy without starting the runtime.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.CaptureCapabilities",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "capture", "capabilities"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.memory.snapshot.request",
      "title": "Request PerfMeter memory snapshot",
      "description": "Request one optional Memory Profiler snapshot with cooldown, free-space guard, requested capture flags, and a correlated evidence bundle. The binary snapshot can contain sensitive process memory.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.MemorySnapshotRequest",
      "risk": "write",
      "idempotency": "unsafe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "memory", "capture", "bundle"],
      "capability": "perfmeter",
      "requires_editor_status": ["playing"],
      "schema": {
        "type": "object",
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 },
          "managed_objects": { "type": "boolean" },
          "native_objects": { "type": "boolean" },
          "native_allocations": { "type": "boolean" },
          "native_allocation_sites": { "type": "boolean" },
          "native_stack_traces": { "type": "boolean" },
          "minimum_free_disk_mb": { "type": "integer", "minimum": 0, "maximum": 1048576 },
          "cooldown_seconds": { "type": "integer", "minimum": 0, "maximum": 86400 }
        },
        "required": ["capture_id"],
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.memory.snapshot.status",
      "title": "Read PerfMeter memory snapshot status",
      "description": "Read memory snapshot and correlated bundle state without starting the runtime or exposing the temporary snapshot path.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.MemorySnapshotStatus",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "memory", "capture", "bundle"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.memory.snapshot.capabilities",
      "title": "Read PerfMeter memory snapshot capabilities",
      "description": "Read optional backend provenance, supported capture flags, snapshot size limit, and owned temporary root without starting the runtime.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.MemorySnapshotCapabilities",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "memory", "capture", "capabilities"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.memory.snapshot.triggers.configure",
      "title": "Configure PerfMeter memory snapshot triggers",
      "description": "Opt in or out of bounded system-memory threshold and leak-growth heuristic triggers. Triggered snapshots use the same cooldown, free-space, sensitivity, and bundle policies as manual requests.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.MemorySnapshotTriggersConfigure",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "memory", "capture", "configuration"],
      "capability": "perfmeter",
      "requires_editor_status": ["playing"],
      "schema": {
        "type": "object",
        "properties": {
          "enabled": { "type": "boolean" },
          "system_memory_threshold_mb": { "type": "integer", "minimum": 0, "maximum": 1048576 },
          "leak_growth_threshold_mb": { "type": "integer", "minimum": 0, "maximum": 1048576 },
          "leak_window_frames": { "type": "integer", "minimum": 30, "maximum": 36000 },
          "managed_objects": { "type": "boolean" },
          "native_objects": { "type": "boolean" },
          "native_allocations": { "type": "boolean" },
          "native_allocation_sites": { "type": "boolean" },
          "native_stack_traces": { "type": "boolean" },
          "minimum_free_disk_mb": { "type": "integer", "minimum": 0, "maximum": 1048576 },
          "cooldown_seconds": { "type": "integer", "minimum": 0, "maximum": 86400 }
        },
        "required": ["enabled"],
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.graphics.diagnostics",
      "title": "Read PerfMeter graphics diagnostics",
      "description": "Read shader GPU-program and graphics-pipeline creation marker values with exact/alias capability provenance and graphics API context. Values retain the discovered recorder unit and are unavailable when Unity does not expose a matching marker.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.GraphicsDiagnostics",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "graphics", "shader", "pso", "diagnostics"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.graphics.state_collection.request",
      "title": "Trace Unity graphics states",
      "description": "Start one bounded Unity GraphicsStateCollection trace during an active PerfMeter session and save an owned project-local .graphicsstate artifact. Session settings control correlated sample density.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.GraphicsStateTraceRequest",
      "risk": "write",
      "idempotency": "unsafe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "graphics", "shader", "pso", "trace"],
      "capability": "perfmeter",
      "requires_editor_status": ["playing"],
      "schema": {
        "type": "object",
        "required": ["capture_id"],
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 },
          "trace_frames": { "type": "integer", "minimum": 1, "maximum": 600 },
          "minimum_free_disk_mb": { "type": "integer", "minimum": 0, "maximum": 1048576 }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.graphics.state_collection.status",
      "title": "Read graphics-state trace status",
      "description": "Read bounded trace/prewarm state, backend provenance, progress, and the owned project-relative artifact path without starting the runtime.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.GraphicsStateCollectionStatus",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "graphics", "pso", "status"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.graphics.state_collection.capabilities",
      "title": "Read graphics-state capabilities",
      "description": "Read Unity GraphicsStateCollection trace/prewarm support, backend provenance, limits, cache-miss support, and parallel PSO creation capability.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.GraphicsStateCollectionCapabilities",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "graphics", "pso", "capabilities"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.graphics.state_collection.cancel",
      "title": "Cancel graphics-state trace",
      "description": "Cancel the matching active Unity GraphicsStateCollection trace and clean its owned pending artifact.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.GraphicsStateTraceCancel",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "graphics", "pso", "trace", "cancel"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "required": ["capture_id"],
        "properties": {
          "capture_id": { "type": "string", "minLength": 1, "maxLength": 128 }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.graphics.state_collection.prewarm",
      "title": "Prewarm Unity graphics states",
      "description": "Load and synchronously prewarm one owned project-local .graphicsstate artifact. Cache-miss evidence is not currently exposed.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.GraphicsStateCollectionPrewarm",
      "risk": "write",
      "idempotency": "unsafe",
      "timeout_ms": 120000,
      "tags": ["perfmeter", "graphics", "shader", "pso", "prewarm"],
      "capability": "perfmeter",
      "requires_editor_status": ["playing"],
      "schema": {
        "type": "object",
        "required": ["relative_path"],
        "properties": {
          "relative_path": { "type": "string", "minLength": 1 },
          "max_state_count": { "type": "integer", "minimum": 0, "maximum": 1000000 }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.device.info",
      "title": "PerfMeter device and environment info",
      "description": "Read Unity device, graphics, display, render pipeline, and environment information.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.DeviceInfo",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "device", "environment"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.camera.snapshot",
      "title": "PerfMeter camera snapshot",
      "description": "Read camera transform, projection, and URP/HDRP camera settings for reproducible performance captures.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.CameraSnapshot",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "camera", "snapshot"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "enum": ["Auto", "MainCamera", "NameFilter", "FirstGameCamera"]
          },
          "camera_name_filter": { "type": "string" }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.rendergraph.snapshot",
      "title": "PerfMeter Render Graph snapshot",
      "description": "Read the latest observed SGG PerfMeter render integration diagnostics for URP Render Graph or HDRP Custom Pass without starting the runtime.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.RenderGraphSnapshot",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "rendergraph", "diagnostics"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.overlay.set",
      "title": "Set PerfMeter overlay",
      "description": "Show or hide the SGG PerfMeter overlay and optionally set its preset, modules, screen corner, theme, display layout, font family, and target FPS. Visibility persists across Play Mode transitions for the current Editor session; rendered_visibility remains unknown until an external capture observes a repainted frame.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.OverlaySet",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "overlay"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "visible": { "type": "boolean" },
          "preset": {
            "type": "string",
            "enum": ["Custom", "Minimal", "Timing", "Rendering", "Memory", "Overdraw", "FullDiagnostics", "AgentDebug"]
          },
          "corner": {
            "type": "string",
            "enum": ["TopLeft", "TopRight", "BottomLeft", "BottomRight"]
          },
          "theme": {
            "type": "string",
            "enum": ["ClassicDark", "Glass", "Cyber", "HighContrast"]
          },
          "layout": {
            "type": "string",
            "enum": ["FpsOnly", "TextCompact", "Graphs", "Classic", "CompactCards", "DiagnosticsWide", "OverdrawFocus", "MetricBars", "Custom"]
          },
          "font_family": {
            "type": "string",
            "enum": ["Manrope", "JetBrainsMono", "LegacyRuntime"]
          },
          "target_fps": {
            "type": "integer",
            "enum": [15, 30, 60, 90, 120, 144, 240]
          },
          "modules": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": ["None", "All", "Fps", "Timing", "Graphs", "Rendering", "SrpBatcher", "Brg", "Uploads", "Memory", "Gc", "GpuMemory", "Overdraw", "Heatmap", "Warnings", "CustomMetrics", "CpuCores", "CpuCoreBars", "CpuCoreGraphs"]
            },
            "minItems": 1,
            "uniqueItems": true
          }
        },
        "required": ["visible"],
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.overdraw.start",
      "title": "Start PerfMeter overdraw measurement",
      "description": "Request a bounded SGG PerfMeter overdraw measurement.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.OverdrawStart",
      "risk": "write",
      "idempotency": "unsafe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "overdraw"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "frame_count": { "type": "integer", "minimum": 1, "maximum": 600 }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.overdraw.cancel",
      "title": "Cancel PerfMeter overdraw measurement",
      "description": "Cancel the current SGG PerfMeter overdraw measurement.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.OverdrawCancel",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "overdraw"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.overdraw.heatmap.set",
      "title": "Set PerfMeter overdraw heatmap",
      "description": "Show or hide the SGG PerfMeter visual overdraw heatmap.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.OverdrawHeatmapSet",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "overdraw", "heatmap"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "visible": { "type": "boolean" }
        },
        "required": ["visible"],
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.session.start",
      "title": "Start PerfMeter session recording",
      "description": "Start bounded SGG PerfMeter session recording with optional warmup, scene-load handling, sample interval, and max sample overrides.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.SessionStart",
      "risk": "write",
      "idempotency": "unsafe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "session"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "warmup_frames": { "type": "integer", "minimum": 0 },
          "warmup_seconds": { "type": "number", "minimum": 0 },
          "sample_interval_seconds": { "type": "number", "exclusiveMinimum": 0 },
          "max_samples": { "type": "integer", "minimum": 1 },
          "reset_on_scene_load": { "type": "boolean" },
          "scene_load_ignore_frames": { "type": "integer", "minimum": 0 },
          "scene_load_ignore_seconds": { "type": "number", "minimum": 0 }
        },
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.session.stop",
      "title": "Stop PerfMeter session recording",
      "description": "Stop the active SGG PerfMeter session recording and return its summary.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.SessionStop",
      "risk": "write",
      "idempotency": "idempotent",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "session"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.session.summary",
      "title": "PerfMeter session summary",
      "description": "Read the current SGG PerfMeter session summary.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.SessionSummary",
      "risk": "read",
      "idempotency": "safe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "session"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "perfmeter.session.export",
      "title": "Export PerfMeter session",
      "description": "Export the current SGG PerfMeter session to a project-local JSON or CSV file.",
      "handler": "SGG.PerfMeter.Editor.Mcp.PerfMeterMcpCommands.SessionExport",
      "risk": "write",
      "idempotency": "unsafe",
      "timeout_ms": 10000,
      "tags": ["perfmeter", "session", "export"],
      "capability": "perfmeter",
      "schema": {
        "type": "object",
        "properties": {
          "path": { "type": "string" },
          "format": { "type": "string", "enum": ["json", "csv"] }
        },
        "required": ["path", "format"],
        "additionalProperties": false
      }
    }
  ]
}
