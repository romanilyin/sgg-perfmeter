# SGG PerfMeter Product Roadmap

Статус: внутренний приоритизированный backlog. Порядок отражает зависимости и продуктовую ценность, но не обещает календарный release scope.

Основание: review текущего backlog и локального research report `C:\Work\Unity\deep-research-report-perfmeter-new-26-8-4.md`, актуализированные для published baseline `2026.8.9-1`; пакет review/RenderDoc research `C:\Work\Unity\perfmeter-temp` является историческим intake для baseline `2026.8.8-1` от августа 2026 года.

## Модель приоритетов

| Priority | Значение |
| --- | --- |
| P0 | Блокирующий дефект или обязательная работа текущего release pass. |
| P1 | Следующая стабилизационная итерация; закрывает текущий пользовательский дефект или высокий compatibility risk. |
| P2 | Стратегическая foundation-работа с высокой продуктовой ценностью после P1. |
| P3 | Следующая самостоятельная feature-итерация после соответствующей foundation. |
| P3.5 | Release-integration cleanup после P3, обязательный до общего build/release pass. |
| P4 | Дорогая или platform-native experimental работа без обязательства ближайшего release. |
| Deferred | Осознанно не входит в ближайшую основную программу. |

## Архитектурные ограничения

- PerfMeter остается low-overhead диагностическим и capture-coordination слоем, а не заменой Unity Profiler, RenderDoc, PIX, Profile Analyzer или Frame Debugger.
- Core package не получает hard dependencies на Adaptive Performance, Memory Profiler или platform-native tooling; интеграции поставляются через optional assemblies/providers/backends.
- Heavy captures и native integrations выключены по умолчанию и требуют явного API, MCP, user action или настроенного alert rule.
- Недоступные метрики и providers возвращают explicit `Unavailable`/degraded state, а не ложный `0`.
- Public API и artifact schemas развиваются additively; старые контракты сохраняются минимум на один стабильный replacement cycle.
- Target invariant для `PM-CORE-001`: heavy serialization, filesystem I/O и hashing не выполняются на Unity main/render thread; Unity-owned state snapshot выполняется до передачи immutable данных worker слою. Replay остается отдельным sidecar concern.
- Capture control, artifact observation/provenance, replay analysis и visualization остаются отдельными слоями с независимыми versioned contracts.
- Принятый [PM-RDOC-001 boundary](../decisions/renderdoc-native-boundary.md) разрешает native RenderDoc только для already-loaded module; package не поставляет, не загружает и не inject RenderDoc/replay binaries, а `.rdc` считается sensitive artifact без automatic upload. Generic Unity `ExternalGPUProfiler` matrix остается отдельным fallback/compatibility path.
- Target invariant для `PM-OBS-004` и будущих rule packs: derived diagnostics не подменяют raw evidence; classification публикует availability, provenance, confidence, coverage, contradicting evidence и verification steps.
- `Nsight` не является единым backend/tool enum: Nsight Systems, Nsight Graphics, Nsight Perf SDK, Aftermath и Nsight Compute имеют независимые optional contracts, availability и product boundaries.
- External tools и SDK остаются user-installed и allowlisted. Editor UI может сохранить выбранный пользователем path только после canonicalization, executable identity/version/capability validation и project-local policy check; MCP/settings JSON не принимают arbitrary executable/DLL paths. Package не устанавливает tools, не повышает права, не включает self-injection и не меняет clocks без отдельного explicit policy.

## Roadmap

| ID | Priority | Status | Scope | Depends On |
| --- | --- | --- | --- | --- |
| `PM-LOG-001` StructuredLog toggle | P0 | resolved, released `2026.8.5-2` | Независимый public toggle отключает только structured info `Debug.Log`, сохраняя callbacks, alerts/history, overlay warnings, EditorWarning и sessions. | - |
| `PM-UI-001` Stable numeric geometry | P1 | resolved, released `2026.8.5-2`, [GitHub #2](https://github.com/romanilyin/sgg-perfmeter/issues/2) | Prefix/value/unit cells, worst-case widths, numeric monospace role, bounded `FpsOnly` fallback, wrapping widgets and geometry tests. | - |
| `PM-UI-002` Owned versioned panel host | P1 | resolved, released `2026.8.5-2`, [GitHub #1](https://github.com/romanilyin/sgg-perfmeter/issues/1) | Owned `UIDocument` host on Unity 6000.4 and `PanelRenderer` on 6000.5+ preserve foreign UI trees/settings and remove only the PerfMeter container. | `PM-UI-001` |
| `PM-CAP-001` Capture coordinator | P2 | resolved, released `2026.8.6-2` | Единый capture domain/state machine, fake backend, overlap guard и deterministic pre/capture/post-roll. Experimental Unity `ExternalGPUProfiler` изолирован для Editor/Development builds, attached external tool и явных RenderDoc/PIX platform/API combinations; completion не заявляет authoritative artifact path. | P1 stabilization |
| `PM-CAP-002` Correlated artifact bundle | P2 | resolved, released `2026.8.6-2` | Versioned atomic bundle связывает отдельно классифицированные baseline/capture samples, alerts, device/camera/render context, optional screenshot и truthful external-artifact observation; project-local quota/retention/redaction policy и MCP request/status/cancel/export/capabilities не заявляют authority без tool-authenticated provenance. | `PM-CAP-001` |
| `PM-COMP-001` Compatibility status and matrix | P2 | resolved, released `2026.8.6-2` | Additive Editor snapshot и structured MCP отдельно сообщают `ImportCompatible` для declared Unity `2022.3` floor, `CoreRuntimeCompatible` для supported Unity `6000.4+` и active URP/HDRP `17.4+` `RenderIntegrationCompatible`; configuration readiness остается отдельной. | P1 stabilization |
| `PM-OBS-001` Dynamic Profiler metric catalog | P2 | resolved, released `2026.8.6-1` | Discover/cache recorder descriptors only at runtime startup and explicit refresh/reconfigure; resolve semantic metrics through exact names and aliases; publish additive API/MCP capability provenance and distinguish unavailable/no-sample/sampled while keeping existing numeric metrics as compatibility values. | P1 stabilization |
| `PM-OBS-002` Profiler instrumentation | P2 | resolved, released `2026.8.6-1` | Internal Scripts-category end-of-frame gauge markers/counters for collection, providers, snapshots, bottleneck, capture/export, CPU/GPU timing, thermal hook and state codes; no public schema changes or overhead subtraction. | `PM-OBS-001` |
| `PM-OBS-003` Self-observability and overhead budgets | P2 | resolved, released `2026.8.6-1` | Fixed 120-frame CPU callback windows для collector, custom providers, CPU-core provider, overlay и URP/HDRP integration; additive API/status/MCP snapshots и per-invocation allocation/performance budgets. GPU attribution остается `Unavailable`, основные CPU/GPU-метрики не корректируются. | `PM-UI-002`, `PM-OBS-002` |
| `PM-PLAT-001` Adaptive Performance telemetry | P2 | resolved, released `2026.8.6-2` | Optional `SGG.PerfMeter.AdaptivePerformance` provider для thermal state/temperature trends, CPU/GPU performance levels, alerts и session/capture samples с provider provenance; core package не получает hard dependency на `com.unity.adaptiveperformance`, а assembly активируется через version define для `5.1.0+` и явно сохраняет unavailable states. | capability/provider seams, `PM-CAP-002` |
| `PM-MEM-001` Memory snapshot trigger | P3 | resolved, released `2026.8.7-1` | Optional Memory Profiler backend для manual/threshold/leak capture с cooldown, free-space guard, capture flags и bundle manifest. | `PM-CAP-001`, `PM-CAP-002` |
| `PM-GFX-001` PSO and shader-stutter diagnostics | P3 | resolved, released `2026.8.7-1` | Dynamic shader/graphics-pipeline creation marker catalog с exact/alias provenance, graphics API и parallel-PSO capability context, а также optional `GraphicsStateCollection` trace/prewarm workflow с active-session correlation, `IsBusy`/`HasPendingCleanup`, StopSession cancellation, persisted owned-cleanup sidecars и bounded project-local artifact. | `PM-OBS-001`, `PM-CAP-001` |
| `PM-REN-001` Render integration context | P3 | resolved, released `2026.8.7-1` | Integration-neutral public snapshot для camera/SRP/pass/freshness/GRD/VRS context через additive compatibility facade. URP сообщает public current-frame rendering mode и фактически scheduled PerfMeter passes; HDRP сообщает observed Custom Pass, но не effective rendering mode. Private RenderGraph pass/resource counters и Editor navigation не заявляются без стабильного public Unity API. | `PM-OBS-001` |
| `PM-GRD-001` GPU Resident Drawer telemetry | P3 | resolved, released `2026.8.7-1` | Показывать GRD/Forward+/compute support, фактическую global runtime activity, aggregate BRG effectiveness counters с provenance и structured fallback/degraded reasons без ложного per-renderer claim. | `PM-OBS-001`, `PM-REN-001` |
| `PM-CI-001` Profile Analyzer and benchmark CI | P3 | resolved, released `2026.8.7-1` | Additive session ID в API/MCP/JSON и последнем CSV-столбце, корреляционные Profiler markers, optional Profile Analyzer opener без hard dependency, versioned zero-allocation/CPU baselines и Unity `6000.4`/`6000.5` CI с NUnit/JUnit/performance artifacts. | `PM-OBS-002` |
| `PM-SESSION-001` Session analysis UI | P3 | resolved, released `2026.8.7-1` | Read-only virtualized Editor UI Toolkit window с retained-sample timeline, worst-frame inspector, derived CPU/GPU budget violations и authoritative whole-run/current-scene scopes без runtime/schema changes. | stable session/artifact schemas |
| `PM-SETUP-001` Setup UX completeness | P3.5 | resolved, baseline released `2026.8.7-1`; FTUE follow-up released `2026.8.7-2`; continuation released `2026.8.8-1` | Полное представление persisted P2/P3 settings, optional integrations и analysis entry points; live FTUE с install/skip/continuation actions; full generated settings bootstrap; read-only compatibility/schema/reserved-metadata/diagnostic states и корректная граница runtime-only inputs. | completed P3 scope |
| `PM-CORE-001` Asynchronous capture-bundle export | P1 | resolved, released `2026.8.9-1` | Main-thread immutable snapshot и async export coordinator с request/status/cancel/progress; worker serialization, streaming artifact copy/hash, atomic staging commit с bounded transient retry, quota/retention и blocking compatibility wrapper без Unity API на worker thread. | `PM-CAP-002` |
| `PM-CORE-002` Allocation-free custom metric collection | P1 | resolved, released `2026.8.9-1` | Cached provider list и reusable core-owned metric buffer; copy только для retained sample/public snapshot/export. Warmed collection path не берет registry lock и не создает compact array. | `PM-OBS-003` |
| `PM-COMP-002` Schema and public-contract compatibility gates | P1 | resolved for core timeline/envelope slice, released `2026.8.9-1` | Existing session/capture schema versions, sample arrays и CSV columns сохранены; explicit enum values, additive timeline/envelope fields и legacy/unknown timeline reader покрыты compatibility tests. Analyzer/native ABI gates остаются привязаны к будущим backend slices. | existing public and artifact schemas |
| `PM-CORE-003` Explicit missing-sample and capture-boundary timeline | P1 | resolved, released `2026.8.9-1` | Additive typed sample-kind/reason codes, explicit missing/capture-boundary markers и session reference на capture bundle; invalid timing не становится `0`, baseline/capture samples не dual-write. | `PM-CAP-002`, `PM-COMP-002` |
| `PM-CI-002` Hosted PlayMode lifecycle gate | P1 | implemented, released `2026.8.9-1`; hosted run blocked/waived pending credentials | Отдельный Unity `6000.4`/`6000.5` PlayMode job и `Tests/PlayMode/**` trigger добавлены; job gated через `PERFMETER_UNITY_CI_ENABLED` и GameCI credentials, fork PR явно получает skipped status. | `PM-CI-001`, repository credentials |
| `PM-OBS-004` Typed diagnostics and stable bottleneck | P2 | design candidate | Additive runtime diagnostic flags, raw warning preservation, observed sample age/freshness и отдельный hysteresis-based stable bottleneck с confidence/evidence; `Unknown` при недостаточной availability. Serialized sample-kind/reason codes остаются scope `PM-CORE-003`. | `PM-CORE-003` |
| `PM-PLAT-002` Platform telemetry cadence and freshness | P2 | design candidate | Bounded cadence/cache для optional platform providers, last-sample age и explicit forced sample at capture boundary без потери `Unavailable` provenance. | `PM-PLAT-001`, `PM-CORE-003` |
| `PM-API-001` Typed mutation results | P2 | design candidate | Additive `TrySet...`/result-returning mutation layer для API и MCP вместо silent `void` no-op; причины rejection/unavailable остаются machine-readable. | public API compatibility policy |
| `PM-UI-003` Widgets, themes and layout descriptors | P4 | design backlog | Расширяемые bounded widgets, semantic theme tokens, manifests, layout descriptors и safety limits без steady-state tree rebuild. | `PM-UI-001`, `PM-UI-002`, `PM-OBS-003` |
| `PM-RG-001` Deeper Render Graph diagnostics | P4 | waiting for stable public APIs | Добавлять pass/resource/aliasing/merge counters только через стабильные Unity APIs; сохранять degraded state вместо reflection. | Unity public APIs |
| `PM-ANDROID-001` Android Perfetto/AGI | P4 | Deferred, deferred by explicit product direction | Low-overhead ATrace/ADPF correlation и Editor sidecar для `adb`, Perfetto config, artifact import и AGI workflow. | `PM-CAP-002`, `PM-PLAT-001` |
| `PM-APPLE-001` Apple thermal, MetricKit and Metal | P4 | Deferred, deferred by explicit product direction | Optional native provider/backend для thermal/low-power/MetricKit и Development-only Metal capture. | `PM-CAP-002`, provider seams |
| `PM-PIX-001` Native PIX timing capture | P4 | experimental candidate | Windows-only circular timing capture, завершаемый после alert, с authoritative artifact provenance. | `PM-CAP-001`, `PM-CAP-002` |
| `PM-EXT-001` External profiling artifact envelope | P4 | resolved, released `2026.8.9-1` | Additive generic envelope для artifact kind, tool/version, tool/bridge-authenticated request association, host namespace, finalized/authority state, source/post-copy hash, `contains_gpu_capture_data`, privacy flags и project-local metadata-only/copy/embed/quota/share policy. Legacy `.rdc`/`.wpix` observation остается non-authoritative. | `PM-CAP-002`, `PM-CORE-001`, `PM-COMP-002` |
| `PM-EXT-002` External profiling lease and conflict coordinator | P4 | resolved, released `2026.8.9-1` | Process-local owner/GPU/operation lease с shared `active-gpu`/`exclusive-profiling-operation` keys и explicit `KnownConflict`/`PossiblyBusy`/`PermissionDenied`/`LostSession`; capture, memory, graphics trace/prewarm и alert lifecycle используют общий coordinator, который не подменяет lifecycle конкретного tool backend. | `PM-EXT-001` |
| `PM-RDOC-001` Native RenderDoc boundary and ADR | P4 | resolved, ADR accepted ([decision](../decisions/renderdoc-native-boundary.md)); docs-only, no support/release claim | Accepted already-loaded-module-only boundary, initial Windows x64 Editor D3D11/D3D12/Vulkan matrix, fixed ABI/lifecycle/failure/authority policy, sensitive `.rdc` metadata-only/copy/embed boundary, separate quota gate, and out-of-process replay non-goal. | `PM-CAP-001`, `PM-CAP-002`, `PM-EXT-001` |
| `PM-RDOC-002` Windows x64 RenderDoc app-API bridge | P4 | resolved, isolated implementation validated; no public wiring/release claim | Auditable fixed C ABI v1 over `GetModuleHandleW`/`RENDERDOC_GetAPI`: 1.7→1.6→1.4 negotiation with required discard support, explicit layout/buffer rules, capabilities, token/count, unique nonce-root template restore, bounded title/comments and path/index/timestamp observation. C/C++ ABI, fake-table and real already-loaded-module negotiation probes pass; no DLL is shipped and no Unity `.rdc` matrix row is claimed. | `PM-RDOC-001` |
| `PM-RDOC-003` Managed RenderDoc backend and artifact provenance | P4 | numeric policy accepted; `PM-RDOC-003B/003C` foundations and package-only `PM-RDOC-003D` worker/lifecycle, handle-safe filesystem, bounded persistent cleanup, retained-Copy export and real Embed source/accounting implemented; test-only D3D11/D3D12/Vulkan `.rdc` matrix passed; production gates and registration pending | Additive async native phases keep the existing capture lease through artifact terminal/freeze; `GenericUnity` remains default, while explicit native fallback is limited to proven pre-begin unavailable results. Stable+hashed files remain non-authoritative until the real-tool gate additionally proves one nonce-root candidate, bridge index/time/file-identity binding, finalized GPU content and source hash, with post-copy hash for Copy/Embed. The accepted [policy](../decisions/renderdoc-storage-policy.md) fixes separate 512 MiB/2 GiB source and Copy/Embed limits, retention, free-space, polling, cleanup/privacy and stall gates before enablement; production registration remains absent until independent title/comments checks, the fixed stall gate and a production-wired post-hardening Windows matrix rerun pass. | `PM-RDOC-002`, `PM-CORE-001`, `PM-EXT-001`, `PM-EXT-002` |
| `PM-RDA-001` RenderDoc analyzer protocol | P4 | design candidate | Независимые versioned request/result/error schemas для action tree, typed counter metadata, raw values, availability distinctions и provenance; unknown units/types round-trip, `uint64` не теряет precision. | `PM-RDOC-003`, `PM-COMP-002` |
| `PM-RDA-002` Out-of-process RenderDoc replay analyzer | P4 | experimental candidate | Sidecar использует отдельно configured RenderDoc installation, проверяет capture hash, получает action tree и bounded core counter batch, публикует actual tool build и поддерживает timeout/cancel/clean shutdown; replay DLL не загружается в Unity process. | `PM-RDA-001`, `PM-EXT-002` |
| `PM-RDA-003` Counter selection planner | P4 | later candidate | Explicit IDs и semantic packs с vendor/API filters, batching/pass-count provenance, safe-category retry и per-counter errors; result различает requested/fetched и не делает silent substitution. | `PM-RDA-002` |
| `PM-RDA-004` Counter aggregation engine | P4 | later candidate | Explicit per-counter sum/min/max/mean/weighted-mean/ratio-of-sums/last/non-aggregatable policy и post-order cache для parent/sibling/frame/capture baselines; name-based implicit aggregation запрещена. | `PM-RDA-001`, `PM-RDA-003` |
| `PM-RDUI-001` RenderDoc importer and virtualized timeline | P4 | later candidate | Safe typed importer/cache и virtualized UI Toolkit action tree для больших captures с raw/aggregate views, availability, session/capture correlation и explicit unmatched data; не вводить TXT/CSV или markup-based contract. | `PM-RDA-002`, `PM-RDA-004`, `PM-CORE-003` |
| `PM-RDRULE-001` Evidence-based GPU diagnostic rules | P4 | deferred until analyzer evidence | Versioned metric catalog и rules с unit/type/vendor/API applicability, aggregation provenance, confidence/coverage/contradicting evidence; relative hotspot percentile не называется hardware saturation, vendor rules требуют validated fixtures. | `PM-RDA-003`, `PM-RDA-004` |
| `PM-RDPROD-001` Self-hosted RenderDoc smoke matrix | P4 | test-only Editor precursor passed; production-like automation remains a later candidate | Manual/nightly Windows D3D11/D3D12/Vulkan smoke запускает sample project under pinned RenderDoc, подтверждает реальный `.rdc`, hash/schema/analyzer results и не загружает sensitive capture artifacts по умолчанию. | `PM-RDOC-003`, `PM-RDA-002` |
| `PM-NSYS-001` Nsight Systems external launcher | P4 | experimental candidate | Discover/version/help probe a user-installed allowlisted `nsys`; a canonicalized Editor-selected path is allowed only after executable validation, never from MCP/JSON. Launch a separate Development Player/Editor with D3D12/Vulkan presets, bounded logs/cancel и project-local finalized `.nsys-rep`; initial evidence matrix records Windows x64, Unity/Editor-or-Player, API, GPU, driver and tool version. | `PM-EXT-001`, `PM-CORE-001`, `PM-COMP-002` |
| `PM-NSYS-002` Optional NVTX bridge | P4 | later candidate | Fixed `SGG.PerfMeter` NVTX domain, cached strings и explicit session/capture/alert ranges; no per-frame default markers, managed allocations или core hard dependency. | `PM-NSYS-001`, `PM-OBS-002` |
| `PM-NSYS-003` Nsight Systems analysis sidecar | P4 | later candidate | Version-aware adapter uses official `nsys analyze/stats/export` into SGG-owned versioned schema through supported jsonlines/SQLite/Arrow outputs; package never directly parses proprietary `.nsys-rep`. | `PM-NSYS-001`, `PM-EXT-001`, `PM-COMP-002` |
| `PM-NSYS-004` Nsight Systems GPU Metrics profile | P4 | later candidate | Separate permission/capability-gated GPU Metrics mode records requested/actual frequency, device-level attribution, concurrent workloads, dropped/overflow/missing ranges and perturbation; data is diagnostic-only and never merged into normal baseline or claimed as process-attributed. | `PM-NSYS-001`, `PM-EXT-002`, `PM-CORE-003` |
| `PM-NGFX-001` Nsight Graphics external workflows | P4 | later candidate | Separate allowlisted `ngfx-capture`/`ngfx.exe` contracts for Graphics Capture and GPU Trace, each with explicit perturbation, host namespace, finalized artifact association and default `DoNotChange` clock policy. Initial matrix records Windows x64, Unity/Editor-or-Player, D3D12/Vulkan, NVIDIA GPU, driver and tool version per workflow. | `PM-EXT-001`, `PM-EXT-002` |
| `PM-NGFX-002` Nsight Graphics procedural trigger bridge | P4 | experimental later candidate | Optional bridge only after external launcher validation; exposes beta SDK/version limitations, one activity per process, no self-injection and no fallback after uncertain begin. | `PM-NGFX-001`, `PM-EXT-002` |
| `PM-NVPERF-001` Nsight Perf SDK capability backend | P4 | deferred candidate | Capability-only user-SDK probe for NVIDIA GPU/SKU/driver/SDK/counter availability; no sampling, clock changes, binary redistribution or support claim derived from research-snapshot versions. | `PM-EXT-002`, `PM-COMP-002` |
| `PM-NVPERF-002` Bounded NVIDIA periodic sampler | P4 | later candidate | Optional Device Periodic Sampler/one-shot backend with native ring buffer, low-rate managed snapshots, dynamic metric provenance and profiling conflict handling; device-level data never claims process attribution. | `PM-NVPERF-001`, `PM-CORE-002`, `PM-EXT-002` |
| `PM-AFTERMATH-001` Nsight Aftermath crash evidence | Deferred | research candidate | Separate crash-safe native backend with preallocated tail, crash artifact and next-launch finalization; not a profiler metric provider and requires explicit crash-product, privacy, EULA and redistribution decisions. | `PM-EXT-001`, crash-product decision |
| `PM-NCU-001` Nsight Compute CUDA workflow | Deferred | CUDA-only boundary | Consider only for a concrete CUDA/OptiX/native CUDA plugin workload; ordinary Unity compute shaders and VFX Graph are explicitly out of scope. | concrete CUDA target |
| `PM-HDRP-001` HDRP overdraw/heatmap parity | Deferred | research only | Отдельный HDRP Custom Pass/shader/readback prototype и device/API matrix; не включать в ранний основной roadmap. | dedicated research validation |
| `PM-OTEL-001` Remote streaming/OpenTelemetry | Deferred | policy undefined | Opt-in batching/export требует transport, credentials, redaction, privacy и support policy до проектирования API. | security/transport decision |

### 2026.8.8-1 Research Intake

- `C:\Work\Unity\perfmeter-temp` подтверждает эволюционный путь: существующие `PM-CAP-001/002`, `PM-OBS-001..003`, `PM-COMP-001`, `PM-CI-001`, `PM-SESSION-001` и `PM-GFX-001` не переоткрываются и не дублируются.
- Source review выявил risks, но не воспроизведенный release blocker, поэтому предложенные research-пакетом core `P0` нормализованы до roadmap `P1`; native/replay work остается `P4` experimental по действующей модели приоритетов.
- Текущий самостоятельный slice реализует `PM-CORE-001..003`, core-часть `PM-COMP-002`, `PM-CI-002` и generic `PM-EXT-001/002`. RenderDoc/Nsight native bridge, launcher, replay protocol/sidecar, SDK и tool-specific backends остаются отдельными будущими итерациями.
- Generic RenderDoc FTUE уже выпущен в `2026.8.8-1`; новые строки относятся только к authenticated native provenance и optional external replay analysis, а не к повторной реализации setup guidance.
- `05-nsight-integration-recommendations.md` не вводит единый Nsight backend: Systems trace, Graphics Capture/GPU Trace, Perf SDK metrics, Aftermath crash evidence и CUDA-only Compute разделены по lifecycle, provenance, licensing и availability.
- Указанные research-версии Nsight являются snapshot, а не support contract. Любая реализация требует dynamic version/help/capability probing, allowlisted user installation и EULA/redistribution review конкретного downloaded SDK package; реальные Nsight tools этим intake не валидированы.

`PM-RDOC-001` accepted evidence: принят в [`renderdoc-native-boundary.md`](../decisions/renderdoc-native-boundary.md) как docs-only ADR. Исходный research был статическим и не являлся support/release claim. Изолированный `PM-RDOC-002` bridge, managed control/interop foundation `PM-RDOC-003B`, production-off storage/finalization foundation `PM-RDOC-003C` и package-only worker/lifecycle/CAS slice `PM-RDOC-003D` реализованы. Test-only real Unity `.rdc` rows пройдены; production registration, native Copy export, hardening и post-hardening matrix остаются gates.

`PM-LOG-001`, `PM-UI-001` и `PM-UI-002` выпущены в `2026.8.5-2`; GitHub issues #1 и #2 закрыты. Внешнее включение StructuredLog toggle остаётся задачей consuming projects и не держит package feature открытой.

`PM-OBS-001`, `PM-OBS-002` и `PM-OBS-003` выпущены в `2026.8.6-1`; Git tag, normal GitHub Release и npm package опубликованы.

`PM-CAP-001` выпущен в `2026.8.6-2`; release-version Unity `6000.4.12f1`/`6000.5.6f1` URP/HDRP full EditMode `172/172` и full PlayMode `13/13` прошли. Attached-tool artifact smoke явно waived из-за отсутствия подключенного RenderDoc/PIX; authoritative artifact result не заявляется.

`PM-CAP-002` выпущен в `2026.8.6-2`; targeted capture bundle EditMode `13/13` и release-version Unity `6000.4.12f1`/`6000.5.6f1` URP/HDRP full suites прошли. Real RenderDoc/PIX artifact smoke явно waived для этого pass; bundle provenance по-прежнему не заявляет неподтвержденную authority.

`PM-COMP-001` выпущен в `2026.8.6-2`; import-only compile на Unity `2022.3.62f1`/`6000.3.20f1` и release-version Unity `6000.4.12f1`/`6000.5.6f1` URP/HDRP full suites прошли.

`PM-PLAT-001` выпущен в `2026.8.6-2`; targeted platform telemetry EditMode `7/7`, telemetry lifecycle PlayMode `1/1` и full matrix прошли. Optional `SGG.PerfMeter.AdaptivePerformance` assembly успешно скомпилирована с `com.unity.adaptiveperformance@5.1.6`; target-device behavior явно waived из-за отсутствия поддерживаемого устройства и не заявляется как проверенное.

`PM-MEM-001` выпущен в `2026.8.7-1`: targeted memory EditMode `9/9`, capture-bundle EditMode `14/14`, PlayMode threshold `1/1`, optional assembly compile с реальным `com.unity.memoryprofiler@1.1.12`, а также Unity `6000.4.12f1` full EditMode `182/182` и full PlayMode `14/14` подтверждены. Windows player startup прошёл; target-device memory behavior явно waived и не заявляется.

`PM-GFX-001` выпущен в `2026.8.7-1`. Unity `6000.4.12f1` compile прошёл; targeted GSC EditMode `25/25`, `PerformanceMeter` API EditMode `47/47`, capture-bundle EditMode `14/14`, PlayMode smoke `12/12`, full post-fix EditMode `208/208` и full post-fix PlayMode `16/16` прошли. Unity `6000.5` tests и Windows player startup прошли; target-device `GraphicsStateCollection` behavior явно waived и не заявляется.

`PM-REN-001` выпущен в `2026.8.7-1`: Unity `6000.4.12f1` main compile passed; targeted `PerformanceMeterApiTests` `53/53`, `PerfMeterCaptureBundleTests` `15/15` и `PerformanceMeterPlayModeSmokeTests` `12/12`; final full EditMode `215/215` и full PlayMode `16/16` прошли. Focused review P1/P2 resolved. URP/HDRP matrix и Windows player startup прошли.

`PM-GRD-001` выпущен в `2026.8.7-1`: Unity `6000.4.12f1` compile passed; targeted `PerformanceMeterApiTests` `58/58`, `PerfMeterCaptureBundleTests` `15/15` и `PerformanceMeterPlayModeSmokeTests` `12/12`; final full EditMode `220/220` и full PlayMode `16/16` прошли. Focused review P1/P2 resolved. URP/HDRP matrix и Windows player startup прошли; target-device GRD/BRG behavior явно waived и не заявляется.

`PM-CI-001` выпущен в `2026.8.7-1`: Unity `6000.4.12f1` compile passed; targeted `PerfMeterSessionCorrelationTests` `5/5`, `PerformanceMeterApiTests` `58/58` и performance tests `2/2` на Unity `6000.4.12f1`/`6000.5.6f1` прошли, warmed instrumentation и session-boundary paths подтвердили `0 B` allocations. Local matrix и player startup прошли; GitHub-hosted execution был явно waived после activation-only failure без Unity test execution, а workflow оставлен opt-in до настройки GameCI-compatible credentials.

`PM-SESSION-001` выпущен в `2026.8.7-1`: Unity `6000.4.12f1` compile passed; targeted `PerfMeterSessionAnalysisTests` `11/11`, `PerformanceMeterApiTests` `58/58`, `PerfMeterSessionCorrelationTests` `5/5` и `PerfMeterCaptureBundleTests` `15/15` прошли; final full EditMode `238/238` и PlayMode `16/16` прошли. Matrix и Windows player startup прошли. Окно не меняет runtime retention/API/schema и не запускает runtime.

`PM-SETUP-001` выпущен в `2026.8.7-1`: Unity `6000.4.12f1` compile прошёл; targeted `PerfMeterSetupWindowTests` `9/9`, `PerfMeterSettingsTests` `22/22`, `PerfMeterSessionAnalysisTests` `11/11`, `PerfMeterMemorySnapshotTests` `9/9` и `PerfMeterGraphicsStateCollectionTests` `25/25` прошли; final full EditMode `247/247` и PlayMode `16/16` прошли. Matrix и Windows player startup прошли. Focused review P1/P2 resolved.

FTUE follow-up `PM-SETUP-001` выпущен в `2026.8.7-2`: targeted `PerfMeterFtueTests` `15/15`, `PerfMeterSetupWindowTests` `11/11`, `PerfMeterSettingsTests` `23/23` и `PerformanceMeterApiTests` `59/59`; full EditMode `266/266` и PlayMode `16/16` прошли на Unity `6000.4.12f1` и `6000.5.6f1`. GitHub Release, npm Trusted Publishing OIDC, `latest`, integrity и SLSA provenance v1 подтверждены.

FTUE continuation `PM-SETUP-001` выпущен в `2026.8.8-1`: guided Memory Profiler, Profile Analyzer, Adaptive Performance, GraphicsStateCollection и RenderDoc continuation actions, а также full generated initialization bootstrap подтверждены targeted `121/121`; full EditMode `279/279` и PlayMode `16/16` прошли на Unity `6000.4.12f1`, `6000.5.6f1`, `6000.6.0b7` и `6000.7.0a4`. GitHub Release, npm Trusted Publishing OIDC, `latest`, registry signature, attestation и SLSA provenance v1 подтверждены.

Released `2026.8.9-1` core hardening `PM-CORE-001..003`, core `PM-COMP-002`, `PM-CI-002` и generic `PM-EXT-001/002`: Unity `6000.4.12f1` compile прошёл; targeted `PerfMeterCaptureBundleTests` `25/25`, `PerformanceMeterApiTests` `74/74` (включая explicit warmed `0 B` custom-metric allocation guard), `PerfMeterTimelineTests` `8/8`, `PerfMeterExternalArtifactTests` `5/5`, `PerfMeterProfilerLeaseTests` `12/12`, `PerfMeterDeviceInfoTests` `12/12` и `PerformanceMeterPlayModeSmokeTests` `12/12` прошли. Focused P1/P2 review resolved после owner-token isolation, memory-snapshot cleanup warning и export-error path-redaction fixes; combined review-fix suite прошёл `111/111`; final full EditMode `324/324` и PlayMode `16/16` прошли. Hosted GameCI PlayMode execution остается blocked/waived до настройки credentials; RenderDoc/Nsight backend, launcher, replay и SDK code не реализовывались и не заявляются. GitHub Release, npm Trusted Publishing OIDC, `latest`, registry SHA-1/integrity, signatures, attestation и SLSA provenance v1 подтверждены.

`PM-RDOC-002` isolated evidence: Windows x64 MSVC `19.51.36252` configured and built the source-only bridge with the pinned RenderDoc header SHA-256; the C ABI compile probe and fake-table suite passed `11/11`, and the DLL exports exactly the six V1 symbols. A production-resolver probe returned `NOT_LOADED` normally, then observed the real already-loaded module/export and negotiated app API `1.7.0` when launched by portable RenderDoc v1.46 from pinned commit `7db2264afa00a5313154022f8c4ae0628a641300`. Unity `6000.4.12f1` import/compile and full EditMode `324/324` passed with `Native~` remaining outside managed compilation. This produced no `.rdc`, did not validate Unity/D3D11/D3D12/Vulkan capture behavior, ships no binary, and is not a support/release claim.

`PM-RDOC-003A` accepted policy evidence: [`renderdoc-storage-policy.md`](../decisions/renderdoc-storage-policy.md) fixes internal numeric defaults and independent accounting for source and Copy/Embed storage, marker-owned retention/cleanup, bounded candidate/stability/finalization windows, native privacy defaults, and the real attached-capture stall gate. This docs-only iteration does not enable managed capture or claim a real `.rdc` result.

`PM-RDOC-003B` control/interop evidence: public backend mode/kind/native-phase fields are additive with fixed values and the released five-argument capture-options constructor remains binary-compatible. `GenericUnity` stays default; `NativePreferred` fallback is restricted to pre-begin result codes `1/2/3/9` with no native resources, `NativeRequired` never falls back, and PIX remains generic-only. The optional Editor-only `SGG.PerfMeter.RenderDoc` assembly mirrors the six-entry C ABI through `sgg_renderdoc_bridge`, while production registration is absent and preflight intentionally fails closed until `PM-RDOC-003D` worker/lifecycle wiring. Generation-bound EOF end, token-bound discard, uncertain begin/end handling, async cleanup polling, lease retention, shutdown/domain-destruction bundle freezing, delayed bridge-candidate re-enumeration, and MCP status fields are covered by focused P1/P2 review with no remaining findings. Windows x64 MSVC build passed; CTest passed `1/1` and the fake-table executable passed `12/12`. Unity `6000.4.12f1` compile passed; targeted coordinator `38/38`, managed interop `32/32`, capture bundle `30/30`, API `74/74`, and PlayMode smoke `12/12` passed; final full EditMode passed `381/381` and full PlayMode passed `16/16`. No bridge binary is shipped, no production native capture is enabled, no `.rdc` was produced, and no D3D11/D3D12/Vulkan support row is claimed.

`PM-RDOC-003C` storage/finalization evidence: the optional assembly now has atomically versioned marker-owned source and Copy roots, canonical containment/reparse rejection, forward-only persisted states, independent 512 MiB item and 2 GiB pool accounting, 16-item/7-day retention, 24-hour stale cleanup, 1 GiB free-space floor, bounded retries, opaque session ownership, and manual-review preservation of unknown contents. A worker-only synchronous finalizer implements repeated bridge enumeration, exact 30-second/500 ms/60-second/180-second monotonic gates, four stable same-handle Windows identity samples, bounded SHA-256, post-hash identity checks, sanitized identity provenance and project-local Copy with post-copy verification; MetadataOnly remains `DoNotShare`, Copy requires `ReviewBeforeShare`, and Embed fails closed until a dedicated native bundle path exists. Bundle attachment is bundle-ID-bound, immutable after export, and rejects authority before `PM-RDOC-003D`. The default production preflight performs no filesystem work and remains fail-closed; no native backend is registered. Focused P1/P2 review passed for this production-off foundation; at that checkpoint path-based deletion/open TOCTOU and cross-worker marker CAS remained explicit `PM-RDOC-003D` registration blockers. Unity `6000.4.12f1` fallback compile passed because baseline `6000.4.7f1` was unavailable; targeted finalizer `13/13`, storage `15/15`, capture bundle `32/32`, external artifact `6/6`, and interop `32/32` passed; final full EditMode passed `412/412` and full PlayMode passed `16/16`. Native source was unchanged from the `PM-RDOC-003B` `1/1` CTest and `12/12` fake-table evidence. No `.rdc` was produced and no support row is claimed.

`PM-RDOC-003D` package-only worker/lifecycle evidence: the internal V3 backend contract now represents pending begin and generation-bound artifact completion, schedules storage preflight/finalization/cleanup on a task worker, retains the capture lease and recording bundle until provenance is frozen, and suppresses stale completion after cancel/destroy. Runtime, active-scene switch, shutdown/domain destruction, begin failure, finalizer exception, Copy rollback and failed-provenance paths are covered. Named per-pool/per-root mutexes, atomic cleanup tombstones and tombstone nonce exclusion serialize marker/quota transitions across workers; unexpected finalizer failure now remains resource-owning until worker cleanup. Focused review also fixed export-state loss and terminal-Copy leakage. Unity `6000.4.12f1` fallback compile passed because baseline `6000.4.7f1` was unavailable; targeted coordinator `38/38`, lifecycle `6/6`, finalizer `14/14`, storage `17/17`, capture bundle `34/34`, interop `32/32`, API `74/74`, external artifact `6/6`, and PlayMode scene switch `1/1` passed; final full EditMode passed `423/423` and full PlayMode passed `17/17`. Unity-generated package/version/quality/preset side effects were removed after validation. Production registration remains absent and default preflight remains fail-closed: a native descriptor/export path for retained Copy payloads, dedicated Embed accounting, and real D3D11/D3D12/Vulkan `.rdc`/stall evidence are still required. Native source was unchanged and no new native build was run. No `.rdc` was produced and no support row is claimed.

`PM-RDOC-003D` handle-safe filesystem hardening evidence: Windows source opens now use `CreateFileW` with `FILE_FLAG_OPEN_REPARSE_POINT`, reject handle-resolved path mismatches, and retain the validated handle for sampling, hashing and Copy reads. Owned cleanup revalidates the exact cleanup marker and the single optional `.rdc` through non-reparse handles, binds every entry to the held root-directory identity, and applies delete disposition through those handles instead of recursive path deletion; a replacement junction is rejected without touching its target. Focused review found and removed a post-delete path re-adoption race. Unity `6000.4.12f1` fallback compile passed; targeted storage `18/18`, finalizer `16/16`, and full EditMode `426/426` passed. Unity-generated package/version/quality/preset side effects were removed after validation. Production registration remains absent, no native source changed, no `.rdc` was produced, and no support row is claimed.

`PM-RDOC-003D` persistent cleanup hardening evidence: a failed immediate three-attempt Abort now retains active-resource ownership while three additional worker sweeps retry only the exact original or atomic tombstone root. Success releases the lease normally; exhaustion terminates the in-process retry instead of looping, while the cleanup marker remains available to next-request/post-reload recovery. Runtime destruction keeps the pending coordinator and bundle frozen across this retry. Focused review bound the retry to the original root so a global sweep cannot misreport unknown content as cleaned. Unity `6000.4.12f1` targeted storage `18/18`, lifecycle `9/9`, interop `32/32`, and final full EditMode `429/429` passed. Unity-generated package/version side effects were removed after validation. Production registration remains absent, no native source changed, no `.rdc` was produced, and no support row is claimed.

`PM-RDOC-003D` retained-Copy descriptor/export evidence: successful finalization now publishes authenticated native metadata only through an internal generation- and bundle-bound descriptor carrying the captured bridge ABI, negotiated app API, boundary/target modes, request nonce, count/index/timestamps and opaque file-identity binding. Copy authority additionally requires equal valid source/post-copy hashes, fixed 512 MiB quota/privacy/share policy, and a retained marker-owned payload source that reopens the exact project-local copy through the handle-safe binding and revalidates terminal ownership, size, identity and SHA-256 at export. The native exporter rejects the legacy caller path, supports authoritative metadata export only through that descriptor, emits additive RenderDoc provenance, excludes `.rdc` bytes from the generic 64 MiB bundle/accounting path, remains cancelable during retained-payload hashing, and never serializes an absolute path. Focused review fixed terminal Copy reservation release, capability snapshot rebinding and UTC-vs-monotonic deadline re-interpretation. Unity `6000.4.12f1` compile passed; targeted finalizer `16/16`, lifecycle `9/9`, capture bundle `36/36`, and final full EditMode `431/431` passed. Unity-generated package/version/quality/preset side effects were removed after validation. Production registration remains absent; dedicated native Embed accounting, comments/title checks, the 2000 ms added-stall gate and a production-wired D3D11/D3D12/Vulkan rerun remain required. Native source was unchanged, no `.rdc` was produced, and no support row is claimed.

`PM-RDOC-003D` fail-closed Embed staging-contract evidence: the generic exporter now accepts pending native Embed authority only through the internal descriptor, writes the ownership marker before payload staging, requires the fixed `external/renderdoc/capture.rdc` and `.sgg-perfmeter-renderdoc` entries, independently rechecks regular-file size and SHA-256 for both, and upgrades authority only after the staged payload hash equals authenticated source evidence. Failed, canceled, conflicting or invalid staging invokes the source abort callback and remains uncommitted; successful atomic commit invokes the terminal callback. Native payload bytes and only those bytes are excluded from the generic 64 MiB per-bundle and aggregate retention accounting, while the manifest and provenance still bind the embedded file. A real 64 MiB + 1 byte sparse payload test proves this separate path without allocating the payload in memory. Production preflight/finalization intentionally still reject Embed until the next iteration supplies marker-owned RenderDoc source retention and unified Copy/Embed pool accounting. Unity `6000.4.12f1` compile passed; targeted capture bundle `38/38`, finalizer `16/16`, lifecycle `9/9`, and final full EditMode `433/433` passed. Unity-generated package/version/quality/preset side effects were removed after validation. No native source changed, no `.rdc` was produced, and no support row is claimed.

`PM-RDOC-003D` real Embed source/accounting evidence: explicit native Embed now retains the terminal marker-owned source until export, reserves the shared Copy/Embed pool before writing, copies through the handle-safe binding into the fixed staging path, independently verifies source and destination identity/size/SHA-256, writes canonical nonce/session/generation/terminal provenance, and commits authenticated authority only through the existing atomic staging contract. Free-space admission includes the payload, 1 MiB reserve and all known non-native staging bytes. Reload-safe accounting scans committed bundles through bounded UTF-8 marker/envelope and safe-tree validation, rejects future/noncanonical records and any nested reparse point, recursively measures the complete owned bundle, revalidates identity before whole-bundle deletion, and orders Copy roots and Embed bundles together under the 2 GiB/16-item/7-day policy. Generic retention may count non-native bundle bytes but never independently delete native Embed bundles. Focused review moved generic quota rejection before native staging and covered free-space, unrelated-junction, identity-replacement and retention-authority regressions. Unity `6000.4.12f1` compile passed; targeted storage `25/25`, finalizer `17/17`, lifecycle `9/9`, capture bundle `40/40`, and final full EditMode `443/443` passed. Unity-generated package/version side effects were removed after validation. Production registration remains absent; independent comments/title checks, the 2000 ms added-stall gate and a production-wired D3D11/D3D12/Vulkan rerun remain required. Native source was unchanged, no `.rdc` was produced, and no support row is claimed.

`PM-RDOC-003D` attached test-only evidence after that package checkpoint: portable RenderDoc v1.46 commit `7db2264afa00a5313154022f8c4ae0628a641300`, app API `1.7.0`, bridge SHA-256 `b77118d48ec4d3873ce2c75d1666b8f3a4493a4a83373001f70dcf1c0bd62583`, Windows 11, NVIDIA GeForce RTX 4060 Ti driver `32.0.16.1062` and Unity `6000.4.12f1` produced retained Copy artifacts through disposable native-required wiring. D3D11, D3D12 and Vulkan exact PlayMode rows each passed `1/1` with `Completed` / `Ready` / `FileObserved`; the `.rdc` SHA-256 values are respectively `76994d77e2f3568a5faad748714261e6f5f4524af2cd257c5a76f2a206879b4d`, `f4eccedf377b568a405381309aaadf60a41a5ab0413b34c14c9cb2bd85d0bf72` and `0d83f0b7f5dbd8882c56ba90095f13ec5aeee079fceafac89b0905dc1d58a4f6`. RenderDoc XML conversion and thumbnail extraction passed for all rows and exposed API-specific draw/clear/present/copy commands. A separate Unity `6000.5.6f1` `SGGSkyAnimatedDemo` D3D11 run passed `1/1`, asserted active sky/volumetric-cloud/froxel diagnostics, produced SHA-256 `f66c649e4d46c0ec2ffa42fc6c70b12123a722567afb04456cd0b5cf183522d7`, and exposed the expected `SGG Sky`, volumetric cloud and froxel GPU markers. Receipts remain local under `C:\Work\Unity\_matrix`; sensitive `.rdc`/XML/thumbnail payloads are not committed. This validates test-only Editor capture behavior, not production registration, title/comments, the 2000 ms added-stall gate or a support row.

Release-version validation for `2026.8.7-1` passed on Unity `6000.4.12f1`/`6000.5.6f1` URP/HDRP, import floors, optional package compiles, Windows player startup, performance tests, and npm dry-runs. Target-device Memory Profiler, `GraphicsStateCollection`, GRD/BRG, and Android physical-device behavior are explicitly waived because the required hardware, Android Build Support, and `adb` are unavailable; no target-device result is claimed. GitHub-hosted Unity execution was explicitly waived because no GameCI-compatible credentials are configured. GitHub Release, npm Trusted Publishing OIDC, `latest`, integrity, and SLSA provenance v1 are verified.

## Release Sequence

| Phase | Scope | Exit Gate |
| --- | --- | --- |
| Stabilization | `PM-UI-001`, `PM-UI-002` | Released in `2026.8.5-2`; Unity 6000.4/6000.5 URP/HDRP editor suites and Windows player smoke passed, with Android device smoke explicitly waived. |
| Capture preview | `PM-CAP-001` | Fake-backend contract tests, no overlap, deterministic state transitions, guarded attached-tool backend and no claim of an authoritative `.rdc`/`.wpix` path from the Unity wrapper. |
| Capture stable | `PM-CAP-002` | Atomic versioned bundle, truthful artifact provenance, session/alert correlation and external-tool smoke matrix. |
| Observability | `PM-OBS-001` through `PM-OBS-003` | Startup-only discovery, capability dump, custom markers/counters and measured overhead budgets. |
| Platform telemetry | `PM-PLAT-001`, then selected P3 integrations | Optional assemblies, no core hard dependency, explicit unavailable states and device validation. |
| Graphics diagnostics | `PM-GFX-001` | Post-fix Unity 6000.4/6000.5 compile and targeted/full EditMode/PlayMode validation, dynamic-marker provenance checks, plus release-player and target-device behavior before release. |
| P3 analysis and setup | `PM-CI-001`, `PM-SESSION-001`, `PM-SETUP-001` | Session correlation/analysis and complete Setup presentation validated before one combined `2026.8.7-1` build/release pass. |
| Core hardening release `2026.8.9-1` | `PM-CORE-001..003`, `PM-COMP-002`, `PM-CI-002` | Async/cancel/restart exporter contracts, core-owned allocation budget, explicit gap/boundary samples, backward schema fixtures and local PlayMode lifecycle evidence; hosted GameCI remained explicitly waived pending credentials, while GitHub and npm publication gates passed. |
| Diagnostics and API stabilization | `PM-OBS-004`, `PM-PLAT-002`, `PM-API-001` | Synthetic hysteresis/freshness/cadence evidence, explicit unavailable states and typed mutation rejection results without silent no-op. |
| External profiling foundation `2026.8.9-1` | `PM-EXT-001`, `PM-EXT-002` | Released in `2026.8.9-1`: versioned finalized-artifact envelope, privacy/host namespace/association fields and deterministic conflict lease remain the foundation before future tool-specific integrations. |
| Nsight Systems optional trace | `PM-NSYS-001`, then `PM-NSYS-002..004` | Independent optional branch after the shared foundation: user-installed launcher precedes bounded NVTX, official-tool analysis and permission-gated device-level GPU Metrics; it is not a sequencing claim over RenderDoc. |
| Platform capture previews | Selected P4 backends | Experimental feature flags, native lifecycle/IL2CPP tests and tool-specific artifact confirmation. |
| Native RenderDoc provenance | `PM-RDOC-001..003` | Accepted ADR first, then isolated Windows already-loaded-module bridge and managed provenance/quota policy; test-only Windows Editor D3D11/D3D12/Vulkan real `.rdc` rows passed, while production-wired reruns, stall/title/comments gates and hardening remain mandatory before any support claim. |
| External replay analyzer | `PM-RDA-001..004` | Versioned golden protocol, out-of-process timeout/cancel/hash checks, action tree, typed counter selection and explicit cached aggregation with truthful availability. |
| RenderDoc timeline and diagnostics | `PM-RDUI-001`, then `PM-RDRULE-001` | Virtualized safe importer/UI precedes evidence-based rule packs; raw values remain available and vendor claims require validated fixtures. |
| Native production hardening | `PM-RDPROD-001` | Self-hosted real `.rdc` matrix validates capture, provenance, schema and analyzer without uploading sensitive captures by default. |
| Nsight Graphics external workflows | `PM-NGFX-001`, then `PM-NGFX-002` | Graphics Capture and GPU Trace stay separate, clocks default to unchanged, and beta procedural SDK work follows validated external launch. |
| NVIDIA metrics prototype | `PM-NVPERF-001`, then `PM-NVPERF-002` | Capability/EULA gate precedes NVIDIA-only bounded sampling; SKU/driver/SDK provenance and device-level attribution remain explicit. |

## Required Gates

- Compile/runtime matrix: latest Unity 6000.4 and 6000.5 patches; import-only checks for the declared import floor and Unity 6000.3.
- General package/render compatibility remains distinct: URP 17.4 and HDRP 17.4, with Windows, Linux, macOS/iOS, and Android API/degraded rows as applicable to each feature.
- The existing generic Unity `ExternalGPUProfiler` requested-tool matrix remains RenderDoc on Windows/Linux desktop with D3D11, D3D12, or Vulkan, and PIX on Windows desktop with D3D12; Unity does not authenticate attached-tool identity or artifact paths.
- The initial native RenderDoc matrix is only Windows x64 Unity Editor on D3D11, D3D12, and Vulkan. Separate attached test-only real smokes passed for each row; production-wired reruns are still required. Development Player, Linux x64, IL2CPP, mobile, and macOS are deferred and not claimed.
- Fake backends are mandatory in automated tests; real RenderDoc/PIX/platform-tool smokes remain gates for their respective future backend slices, not evidence supplied by this ADR.
- Adaptive Performance `5.1+` package integration and real target-device thermal/performance validation remain release-candidate gates; fake providers and contract tests do not replace them.
- Memory Profiler `1.1.0+` real-package integration, release-player behavior, and target-device validation remain the `PM-MEM-001` release gate; targeted tests and the `1.1.12` optional compile check do not replace it.
- `PM-GFX-001` release gate remains the Unity 6000.4/6000.5 namespace/assembly matrix, full Unity 6000.5 tests, release-player behavior, and target-device checks. The final Unity `6000.4.12f1` evidence above validates the post-fix lifecycle, dynamic marker provenance, active-session correlation, overlap/cancel/prewarm cleanup, and 64 MiB owned-artifact limit, but does not replace those release gates.
- Capture samples are classified separately from normal baseline samples; capture overhead must not silently contaminate normal performance evidence.
- Steady-state collector and hidden overlay target `0 B/frame`; dynamic discovery runs only at startup/reconfigure and export stays outside frame-critical paths.
- Bundles enforce project-local path validation, atomic commit, disk quota/retention and redaction of sensitive paths, screenshots and device metadata.
- Async export snapshots Unity-owned state on the main thread, then keeps serialization/filesystem/hash work off main/render threads; 512 MiB synthetic artifacts, cancel/retry, quota conflicts and owned stale-staging cleanup are mandatory gates.
- Invalid timing and capture boundaries remain explicit typed timeline events, never numeric zero; requested capture frame counts are reconstructable without dual-writing capture samples into the baseline session.
- Every changed public/bundle/analyzer/native contract has golden fixtures, additive/unknown-field tests and explicit enum values. Persisted bundle changes require v1-to-current backward-reader fixtures; analyzer schemas and native ABI each require their own independent version/baseline tests.
- Native RenderDoc may access only an already-loaded module and must not ship/inject/load RenderDoc. API negotiation is `1.7→1.6→1.4`; required discard support excludes older APIs. A stable+hashed unverified file maps to `AssociationState.Unverified`, `FinalizationState.Finalized`, and `AuthorityState.Observed`, never authority; native authority additionally requires exactly one candidate under a unique nonce root, bridge index/time and stable file-identity binding, `AssociationState.BridgeAuthenticated`, finalized `ContainsGpuCaptureData = Present`, and a valid source SHA-256, while Copy/Embed also require a post-copy hash. `ToolAuthenticated` remains reserved for stronger tool-provided association.
- Replay remains out-of-process with configured external tooling, capture SHA verification, bounded output, timeout/cancel and actual tool-build provenance; unsupported/not-requested/fetch-failed/available-zero remain distinct states.
- Timeline and diagnostic gates include safe text rendering, virtualized large-capture behavior and raw evidence retention; correlation or relative p90 hotspots are never presented as causal hardware-saturation proof.
- External GPU artifacts publish metadata-only/copy/embed mode, finalized state, host namespace, `contains_gpu_capture_data`, separate quota and export/share warning; no extension/hash-only result grants authority or permits automatic upload. Native `.rdc` defaults to `MetadataOnly` + `DoNotShare`, exposes only relative project-local metadata, and requires explicit `ReviewBeforeShare` for export. The accepted `PM-RDOC-003A` policy fixes separate 512 MiB per-file and 2 GiB aggregate source and Copy/Embed pools, 16-item/7-day retention, a 1 GiB post-write floor, bounded observation/finalization, marker-owned retries, and the 2000 ms real-capture stall gate.
- RenderDoc capability vocabulary is future-additive: static eligibility, module loaded, API negotiated, capture ready, and provenance available. Existing `renderdoc_supported` remains compatibility/static eligibility only and does not verify the requested tool identity.
- The completed `PM-RDOC-002`, `PM-RDOC-003B`, production-off `PM-RDOC-003C`, and package-only `PM-RDOC-003D` worker/lifecycle/CAS/handle-safe/persistent-cleanup/retained-Copy/real-Embed subsets cover fake module/export/API/discard/count/ambiguous-candidate/path-buffer/UTF-8/template/concurrency cases, fixed C/C++/managed ABI baselines, delayed candidate re-enumeration, explicit fallback and uncertainty handling, marker ownership/accounting/retention, bounded observation/stability/hash/Copy/Embed provenance, generation-bound worker completion and descriptor export, lease/bundle freeze, scene/domain teardown, cross-worker marker/quota serialization, handle-bound source open/owned cleanup race rejection, bounded exact-root cleanup retries across runtime destruction, retained payload mutation and legacy caller-path rejection. Test-only real Unity D3D11/D3D12/Vulkan capture, path/hash, API commands and thumbnails are validated. Remaining gates include production registration, independent comments and capability-conditional API `1.6+` title checks, the fixed 2000 ms added-stall gate, and a production-wired matrix rerun. No support row is claimed.
- `PM-OBS-004` requires oscillating/insufficient-evidence hysteresis tests; `PM-PLAT-002` requires cadence, last-sample-age and forced-capture-sample tests; `PM-API-001` requires machine-readable rejected/unavailable results and no silent MCP no-op.
- External tools are discovered only from allowlisted user installations with dynamic version/help probing. A canonicalized Editor-selected path requires executable identity/version validation; MCP/settings JSON cannot submit paths. Package never installs/elevates, enables self-injection, changes clocks or uploads reports automatically.
- Every Nsight tool slice publishes an explicit validated matrix before support claims: Unity version, Editor/Development Player, OS/architecture, graphics API, GPU/SKU, driver, tool/SDK version and privilege/clock policy. Research-snapshot versions are not support ranges.
- Nsight Systems gates include process cancel/log bounds, tool-reported finalized `.nsys-rep`, host namespace and NVTX warm-path allocation/overhead tests; proprietary reports are consumed only through supported official export/analyze surfaces. GPU Metrics additionally validates permissions, requested/actual frequency, device-level `process_attribution_available=false`, concurrent workloads and dropped/overflow/missing ranges.
- Nsight Graphics gates keep Graphics Capture and GPU Trace as separate exclusive activities, preserve default `DoNotChange` clocks, record measurement perturbation and treat procedural SDK `0.9.x` assumptions as beta until revalidated.
- Nsight Perf SDK gates require NVIDIA GPU/SKU/driver/SDK/counter capability provenance, profiling-conflict handling, EULA/redistribution review and explicit device-level `process_attribution_available=false`; missing/vendor-inapplicable metrics never become zero.
- If `PM-AFTERMATH-001` is reactivated, crash-path work requires preallocated crash-safe storage, next-launch finalization, separate privacy/quota policy and no profiler-metric claim.

## Not Planned

- Built-in Render Pipeline support remains unsupported and is not planned.
- XR/world-space overlay remains outside scope until a concrete supported target exists.
- RenderDoc runtime replay inside Unity, bundled/injected RenderDoc binaries, automatic `.rdc` upload and direct LLM diagnosis from raw captures are outside this initiative.
- Nsight self-injection, automatic install/elevation, bundled SDK/tool binaries, arbitrary tool paths, Perf SDK Range Profiler and unified RenderDoc/Nsight counter schemas are outside the current initiative.
- Nsight Compute is not used for ordinary Unity compute shaders or VFX Graph; Aftermath remains crash evidence rather than a performance-metric provider.

## Detailed Backlogs

- UI ownership, geometry, widgets and themes: `ui-widgets-and-themes.md`.
- Collector, sessions, self-overhead and rendering diagnostics: `profiler-backlog.md`.
- HDRP status and deferred overdraw work: `hdrp-support.md`.
- Resolved PerfMeter-owned MCP reports: `mcp-problem-reports.md`.
