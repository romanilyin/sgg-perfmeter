# 2026.8.5-2 UX_0 Release

Status: release candidate prepared; not published.

## Scope

- Version/tag: `2026.8.5-2`.
- Package: `com.sungeargames.perfmeter`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- Built-in Render Pipeline remains unsupported.
- Validation in this implementation pass targets Unity `6000.5.6f1`; the full version/platform matrix is a separate final release gate.

## Release Notes

- Added `PerformanceMeter.StructuredLogsEnabled` and `PerformanceMeter.SetStructuredLogsEnabled(bool)` as an independent structured alert-log control.
- Added stable numeric prefix/value/unit cells, monospace numeric rendering, bounded `FpsOnly` fallback and wrapping cards/budget bars.
- Added a dedicated owned UI Toolkit host with `UIDocument` on Unity 6000.4 and `PanelRenderer` on Unity 6000.5+.
- Preserved foreign UI Toolkit settings and children and limited rebuild removal to the PerfMeter-owned container.
- Added PlayMode coverage for responsive geometry, max-font widget bounds, numeric width stability, host ownership and repeated lifecycle/rebuild operations.
- Updated all supported localized user documentation for the new API and overlay behavior.

## Validation

- [x] Unity `6000.5.6f1` compile passed after implementation and focused review fixes.
- [x] Focused overlay PlayMode tests: `4/4` passed.
- [x] Existing PlayMode smoke tests: `5/5` passed.
- [x] `PerformanceMeterApiTests`, including StructuredLog behavior: `47/47` passed.
- [x] Full EditMode suite on Unity `6000.5.6f1`: `89/89` passed.
- [x] Full PlayMode suite on Unity `6000.5.6f1`: `9/9` passed.
- [x] `npm pack --dry-run --json` passed; package-content review covered all `178` packed files.
- [x] `npm publish --dry-run --access public --json` passed; npm login remains required before the real publish.
- [ ] npm authentication verified for the real publish.
- [ ] Unity 6000.4/6000.5, URP/HDRP and player/device release matrix.

## Release Steps

1. Verify npm authentication for the real publish.
2. Run the separate version/platform/player matrix and record authoritative results here.
3. Merge `feature/UX_0` through the normal review path to `main`.
4. Tag `2026.8.5-2`.
5. Create a normal GitHub Release for tag `2026.8.5-2`.
6. Publish `com.sungeargames.perfmeter@2026.8.5-2` to npm with public access.
7. Close GitHub issues #1 and #2 after the GitHub and npm releases are published.
8. Update public README/install pins from `2026.7.19-1` to `2026.8.5-2` only after the tag and npm package exist.
