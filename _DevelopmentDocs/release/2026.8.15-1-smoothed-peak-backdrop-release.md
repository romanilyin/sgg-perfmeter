# 2026.8.15-1 Smoothed Peak Backdrop Release

Status: release candidate.

## Scope

- Version/tag: `2026.8.15-1`.
- Package: `com.sungeargames.perfmeter`.
- Release work date: `2026-08-15`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- Implements roadmap item `PM-UI-009` for the existing smoothed CPU and GPU timing graphs.

## Release Notes

- Per-frame CPU/GPU timing samples accumulate a valid raw maximum between throttled history-graph samples.
- Semantic-red peak segments render behind the existing graph line/fill and only the excess above the foreground shape remains visible.
- Horizontal compression selects the maximum valid raw peak in each rendered pixel bucket, so a one-frame spike is not averaged away.
- Raw peaks are projected and clipped into the already selected vertical range. They do not participate in scale calculation and cannot change scale labels, budget lines, or foreground coordinates.
- Invalid, unavailable, non-finite, duplicate, and stale timing samples do not create peak evidence.

## Validation

- [x] Unity `6000.4.12f1` implementation compile passed.
- [x] Targeted `PerfMeterFrameTimeStripTests` passed `20/20`.
- [x] Targeted `PerfMeterOverlayPlayModeTests` passed `7/7`.
- [x] Unity `6000.4.12f1` release-version compile passed; full EditMode passed `539` with one opt-in RenderDoc replay smoke ignored out of `540`, and full PlayMode passed `20/20`.
- [x] Unity `6000.5.6f1` release-version compile passed; full EditMode passed `539` with the same opt-in test ignored out of `540`, and full PlayMode passed `20/20`.
- [x] Unity `6000.6.0b7` release-version compile passed; full EditMode passed `539` with the same opt-in test ignored out of `540`, and full PlayMode passed `20/20`.
- [x] Unity `6000.7.0a4` release-version compile passed; full EditMode passed `539` with the same opt-in test ignored out of `540`, and full PlayMode passed `20/20`.
- [x] Focused implementation and release review found no remaining actionable findings after invalid, unavailable, focus-loss, and capture-cleanup gaps were made to terminate pending raw-peak intervals.
- [x] `git diff --check`, `npm pack --dry-run --json`, and `npm publish --dry-run --access public --json` passed; publish dry-run does not validate OIDC authentication.
- [x] Package-content review covered `362` files, `1007823` packed bytes, and `4818797` unpacked bytes with no generated project state, native or RenderDoc binaries, captures, local logs, Python bytecode, or credentials.
- [x] Dry-run package SHA-1 is `c6ab75e331a212168e0e737a48f7f39152f57d35`; integrity is `sha512-xvrHjdwEFAOltYiJQkRSLA6sP+k0H4iFET0GCEPpE4zilDGDtCTXVfEPxO8/qLEHJtSD9q1MVJuyq7qAOIMuxg==`.

## Boundaries

- This release uses the explicitly requested one-time Unity `6000.4` through `6000.7` validation matrix. Subsequent PerfMeter releases return to one primary Unity validation version unless a broader matrix is explicitly requested.
- The raw peak backdrop is a default visual refinement. It adds no public API, preset/schema field, setting, or user toggle.
- Peak evidence changes only graph rendering. It does not change raw timing metrics, alerts, spike counters, session/capture data, smoothing/sampling semantics, or the separate RAW strip.
- The optional Windows x64 RenderDoc bridge remains the separately published `2026.8.11-1` artifact; no native artifact is published for this release.
- Repository-facing npm and Git UPM install examples remain on published `2026.8.13-2` until GitHub/npm publication, provenance, and clean-consumer verification complete. The immutable release tag will retain those prepublication pins by design.

## Publication Gates

- [ ] Release PR [#35](https://github.com/romanilyin/sgg-perfmeter/pull/35) merged to `main`.
- [ ] Immutable annotated tag `2026.8.15-1` points to the exact main merge commit.
- [ ] Normal GitHub Release is published for the exact tag.
- [ ] npm Trusted Publishing OIDC publishes `com.sungeargames.perfmeter@2026.8.15-1` and moves `latest`.
- [ ] Registry integrity, signature, attestation, and SLSA provenance are verified.
