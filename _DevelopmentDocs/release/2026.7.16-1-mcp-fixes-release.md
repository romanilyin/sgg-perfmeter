# 2026.7.16-1 MCP Fixes Release

Status: release pass.

## Scope

- Version/tag: `2026.7.16-1`.
- Package: `com.sungeargames.perfmeter`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- Built-in Render Pipeline remains unsupported.

## Release Notes

- Made MCP session export file commits atomic and existing-file conflicts deterministic.
- Added owning package version provenance and export schema v2 configured/effective settings snapshots.
- Added explicit requested, active-component, deferred, and unobserved rendered overlay state.
- Preserved MCP overlay visibility across Play Mode transitions for the current Editor session.
- Detached hidden overlay UI before repaint requests and retained external capture as the rendered-pixel authority.
- Removed the obsolete panel font API warning on Unity 6000.5+ while retaining older-editor compatibility.

## Validation

- Unity `6000.5.3f1` compile: passed without PerfMeter package warnings or errors.
- EditMode: `84/84` passed.
- PlayMode: `5/5` passed.
- `npm pack --dry-run --json`: passed, 172 files.
- Authenticated `npm publish --dry-run --access public`: passed.
- GitHub and npm publish access: verified for `romanilyin`.

## Release Steps

1. Complete local gates and commit release metadata.
2. Merge `feature/mcp_report_fixes` through a pull request to `main`.
3. Tag `2026.7.16-1`.
4. Publish `com.sungeargames.perfmeter@2026.7.16-1` to npm with public access.
5. Create a normal GitHub Release for tag `2026.7.16-1`.
