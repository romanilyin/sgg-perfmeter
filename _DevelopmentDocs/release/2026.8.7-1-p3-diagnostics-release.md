# 2026.8.7-1 Combined P3/P3.5 Diagnostics Release

Status: release candidate prepared; not published.

## Scope

- Version/tag: `2026.8.7-1`.
- Package: `com.sungeargames.perfmeter`.
- Release work date: `2026-08-07`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- P3/P3.5 scopes: `PM-MEM-001` optional memory snapshots, `PM-GFX-001` shader/PSO diagnostics and `GraphicsStateCollection` trace/prewarm, `PM-REN-001` integration-neutral render context, `PM-GRD-001` truthful GRD/BRG telemetry, `PM-CI-001` session IDs/Profiler correlation/Profile Analyzer opener/performance CI, `PM-SESSION-001` read-only Session Analysis UI, and `PM-SETUP-001` complete Setup UX.
- Built-in Render Pipeline remains unsupported.
- HDRP overdraw and heatmap remain unsupported; core diagnostics remain available.

## Release Notes

- Added optional `PM-MEM-001` Memory Profiler snapshot triggers for manual, threshold, and leak workflows with cooldown, free-space guards, capture flags, and bundle manifest metadata.
- Added `PM-GFX-001` shader/PSO and graphics-pipeline diagnostics plus optional `GraphicsStateCollection` trace/prewarm lifecycle handling with capability provenance, session correlation, cancellation, and owned cleanup.
- Added the integration-neutral `PM-REN-001` render context snapshot for camera, SRP/pass, freshness, GRD, and VRS context while keeping URP/HDRP public-API boundaries explicit.
- Added truthful `PM-GRD-001` GRD/Forward+/compute support, global activity, and aggregate BRG effectiveness telemetry with provenance and degraded reasons rather than per-renderer claims.
- Added `PM-CI-001` session IDs, Profiler correlation markers, an optional Profile Analyzer opener without a hard dependency, and versioned performance baseline/CI artifact surfaces.
- Added the read-only `PM-SESSION-001` virtualized UI Toolkit Session Analysis window with retained-sample timeline, worst-frame inspection, derived budget violations, and whole-run/current-scene scopes.
- Completed `PM-SETUP-001` Setup UX presentation for persisted P2/P3 settings, optional integrations, analysis entry points, and read-only compatibility/schema/diagnostic states with runtime-only boundaries.

## Validation

### Checked feature-final evidence

- [x] Unity `6000.4.12f1` compile passed for the feature-final implementation surface.
- [x] Targeted `PerfMeterSetupWindowTests` `9/9` passed.
- [x] Targeted `PerfMeterSettingsTests` `22/22` passed.
- [x] Targeted `PerfMeterSessionAnalysisTests` `11/11` passed.
- [x] Targeted `PerfMeterMemorySnapshotTests` `9/9` passed.
- [x] Targeted `PerfMeterGraphicsStateCollectionTests` `25/25` passed.
- [x] Feature-final full EditMode `247/247` and full PlayMode `16/16` passed.
- [x] Isolated Unity `6000.5.6f1` package consumer compile passed.
- [x] Focused P1/P2 review resolved with no remaining findings.

### Checked candidate-version gates

- [x] Unity `6000.4.12f1` URP full EditMode `247/247` and PlayMode `16/16` passed.
- [x] Unity `6000.5.6f1` URP correctness EditMode `245/245`, performance EditMode `2/2`, and PlayMode `16/16` passed.
- [x] Unity `6000.4.12f1` HDRP correctness EditMode `245/245` and PlayMode `16/16` passed.
- [x] Unity `6000.5.6f1` HDRP correctness EditMode `245/245` and PlayMode `16/16` passed.
- [x] Import-only package compilation passed on Unity `2022.3.62f1` and `6000.3.20f1`.
- [x] A Unity `6000.5.6f1` consumer compiled the candidate with `com.unity.memoryprofiler@1.1.12`, `com.unity.adaptiveperformance@5.1.6`, `com.unity.performance.profile-analyzer@1.4.0`, `SGG.PerfMeter.GraphicsStateCollection`, and the performance-test assembly enabled together.
- [x] Unity `6000.5.6f1` URP and HDRP Windows Development players built and completed 20-second headless startup smokes without exceptions or crashes.
- [x] `npm pack --dry-run --json` passed; package-content review covered `256` files, `731684` packed bytes, and `3225516` unpacked bytes.
- [x] Local `npm publish --dry-run --access public --json` package-content check passed; local dry-run does not validate OIDC authentication.
- [x] Focused release metadata and pipeline-neutral test review found no P1/P2 defects.
- [x] GitHub-hosted Unity `6000.4.12f1`/`6000.5.6f1` correctness and performance CI was attempted in [Actions run `31182497790`](https://github.com/romanilyin/sgg-perfmeter/actions/runs/31182497790) but executed no Unity tests because the repository has no GameCI-compatible account credentials. This infrastructure gate is explicitly waived for this release; the checked local URP/HDRP version matrix above remains the authoritative evidence. Hosted Unity jobs remain opt-in through `PERFMETER_UNITY_CI_ENABLED=true` after compatible credentials are configured.
- [ ] Normal GitHub Release publication for tag `2026.8.7-1`, npm publication, `latest`, integrity, and provenance verification remain pending.

### Device and tool evidence boundaries

- [x] Target-device Memory Profiler behavior is explicitly waived because no supported validation device is attached; only optional-package compilation, fake-backend contracts, Editor tests, and Windows startup are claimed.
- [x] Target-device `GraphicsStateCollection` behavior is explicitly waived because no supported validation device is attached; only typed backend compilation, fake-backend contracts, Editor tests, and Windows startup are claimed.
- [x] Target-device GRD/BRG telemetry behavior is explicitly waived because no supported validation device is attached; no per-renderer effectiveness or target-device result is claimed.
- [x] Android physical-device smoke is explicitly waived because Unity Android Build Support, `adb`, and an attached release device are unavailable.
- No attached RenderDoc/PIX or other native graphics-tool artifact result is claimed by this candidate record.

## Release Steps

1. Complete every unchecked candidate-version, review, publication, and verification gate above.
2. Complete the final metadata review and merge the release metadata change to `main`.
3. Create and push the immutable tag `2026.8.7-1` from the release merge commit.
4. Publish a normal GitHub Release for the exact tag.
5. Approve the owner-gated npm Trusted Publishing OIDC run and verify the exact candidate publication before any recovery attempt.
6. Verify the npm version, `latest`, tarball, integrity, and provenance for `com.sungeargames.perfmeter@2026.8.7-1`.
7. Update public npm and Git UPM install pins from published `2026.8.6-2` to `2026.8.7-1` only after GitHub and npm publication both exist and are verified.
