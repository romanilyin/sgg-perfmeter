# 2026.7.19-1 Alert Provenance And Player UI Release

Status: release pass.

## Scope

- Version/tag: `2026.7.19-1`.
- Package: `com.sungeargames.perfmeter`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- Built-in Render Pipeline remains unsupported.

## Release Notes

- Added lifecycle, steady-state, and explicit capture classification to alert snapshots and fired-history counters.
- Added alert-history interval, start, reset-reason, and latest-fired provenance to runtime and MCP reads.
- Added bounded `perfmeter.alerts.capture.begin/end` commands for authoritative external capture attribution.
- Replaced runtime-created UI Toolkit panel/text/theme objects with a serialized package resource containing Unity-assigned ICU data.
- Removed runtime theme discovery and its main-thread `FindObjectsOfTypeAll` fallback.

## Validation

- Unity `6000.5.4f1` compile: passed without PerfMeter package warnings or errors.
- EditMode: `87/87` passed.
- PlayMode: `5/5` passed.
- Unity `6000.4.10f1` import/compile: passed without PerfMeter package warnings or errors.
- Windows x64 Development Player: 15-second overlay smoke passed with zero ICU, main-thread text fallback, ATG text-job, or UI Toolkit layout exceptions.
- `npm pack --dry-run --json`: passed, 174 files.
- Authenticated `npm publish --dry-run --access public`: pending.

## Release Steps

1. Complete package dry-runs and commit release metadata.
2. Merge `release/perfmeter-2026.7.19-1` through a pull request to `main`.
3. Tag `2026.7.19-1`.
4. Create a normal GitHub Release for tag `2026.7.19-1`.
5. Publish `com.sungeargames.perfmeter@2026.7.19-1` to npm with public access.
