# 2026.8.8-1 FTUE Continuation Release

Status: release candidate.

## Scope

- Version/tag: `2026.8.8-1`.
- Package: `com.sungeargames.perfmeter`.
- Release work date: `2026-08-08`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- Added guided post-install and external-tool continuation actions for Memory Profiler, Profile Analyzer, Adaptive Performance, RenderDoc, and GraphicsStateCollection.
- Added a complete self-contained generated bootstrap through `PerformanceMeter.TryApplySettingsJson(...)`, including logging, alerts, session defaults, overdraw, collection, and visual settings.
- Made valid explicit settings deterministic over Resources auto-start while keeping invalid JSON non-mutating.
- Added localized workflow guidance and copyable runtime snippets without automatically starting sessions, snapshots, traces, prewarm, or GPU captures.

## Release Notes

- Installed optional package rows now explain and expose their next usable action instead of ending at `Available`.
- RenderDoc guidance covers installation, Unity's `Load RenderDoc` flow, launch-through-RenderDoc fallback, attachment checks, capture requests, and the external-profiler identity/artifact limitations.
- Memory Profiler provides one-shot and runtime-trigger snippets plus snapshot-folder reveal; Profile Analyzer documents the required Unity Profiler recording order.
- GraphicsStateCollection provides trace/prewarm snippets, Runtime status, artifact reveal, and the required session-to-artifact-to-prewarm sequence.
- Initialization Code embeds the normalized Project Settings snapshot, including Editor warning logs and structured logs, and remains separate from explicit capture/profiling operations.

## Validation

### Feature and release gates

- [ ] Unity `6000.4.12f1` compile passed.
- [ ] Unity `6000.5.6f1` compile passed.
- [ ] Unity `6000.6.0b7` compile passed.
- [ ] Unity `6000.7.0a4` compile passed.
- [ ] Targeted EditMode tests passed for `PerfMeterFtueTests`, `PerfMeterSetupWindowTests`, `PerfMeterSettingsTests`, and `PerformanceMeterApiTests`.
- [ ] Full EditMode passed on Unity `6000.4.12f1`.
- [ ] Full EditMode passed on Unity `6000.5.6f1`.
- [ ] Full EditMode passed on Unity `6000.6.0b7`.
- [ ] Full EditMode passed on Unity `6000.7.0a4`.
- [ ] Full PlayMode passed on Unity `6000.4.12f1`.
- [ ] Full PlayMode passed on Unity `6000.5.6f1`.
- [ ] Full PlayMode passed on Unity `6000.6.0b7`.
- [ ] Full PlayMode passed on Unity `6000.7.0a4`.
- [ ] All ten XLIFF files parse and expose identical source sets.
- [ ] Focused review has no remaining P1/P2 findings.
- [ ] `git diff --check` passed.
- [ ] `npm pack --dry-run --json` passed and package contents were reviewed.
- [ ] `npm publish --dry-run --access public --json` passed; this does not validate OIDC authentication.
- [ ] Package metadata, assembly metadata, version tests, and both changelogs agree on `2026.8.8-1`.

### Publication gates

- [ ] Release PR merged to `main` with required owner approval and resolved conversations.
- [ ] Immutable tag `2026.8.8-1` created at the release merge commit.
- [ ] Normal GitHub Release published for the exact tag.
- [ ] Owner-gated Trusted Publishing OIDC workflow completed successfully.
- [ ] npm `latest`, tarball integrity, signature, attestation, and provenance verified.
- [ ] Public npm and Git UPM install pins updated only after GitHub and npm publication verification.

## Boundaries

- Dynamic diagnostics can contain raw runtime identifiers, paths, provider warnings, and measured values; those values remain untranslated. Static continuation guidance, labels, buttons, and tooltips are localized.
- The `6000.6.0b7` and `6000.7.0a4` rows are local compatibility evidence for installed pre-release Editors; they do not redefine Unity's stability or support status.
- Native RenderDoc/PIX artifacts, physical devices, Android players, and the broader optional consumer matrix are not newly claimed by this Editor/runtime workflow release.

## Publication Record

Not published yet.
