# 2026.8.11-1 RenderDoc Bridge Release

Status: release candidate.

## Scope

- Version/tag: `2026.8.11-1`.
- Package: `com.sungeargames.perfmeter`.
- Release work date: `2026-08-11`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- Native support row: Windows x64 Unity Editor with RenderDoc on Direct3D 11, Direct3D 12, or Vulkan.
- Optional artifact: `sgg-perfmeter-renderdoc-bridge-2026.8.11-1-windows-x64.zip` plus its `.sha256` file, distributed separately from the binary-free UPM package.

## Release Notes

- Added verified FTUE download, local-file installation, update, cancel, status, and managed removal for the separately published Windows x64 Editor bridge. The installer accepts only the exact package-pinned size, SHA-256, and native AMD64 PE contract and configures every player target off.
- RenderDoc remains user-installed and user-owned. The bridge resolves only an already-loaded `renderdoc.dll`; it never installs, loads, injects, launches, or bundles RenderDoc or replay binaries.
- Added explicit `GenericUnity`, `NativePreferred`, and `NativeRequired` request modes. The generic Unity path remains the compatibility default and cannot authenticate attached-tool or artifact identity.
- The native path carries generation-bound lifecycle and authenticated provenance into MetadataOnly, Copy, or Embed storage. MetadataOnly defaults to `DoNotShare`; Copy and Embed require explicit review before sharing and use independent limits, retention, ownership, cleanup, and hash verification.

## Validation

### Package and installer

- [x] Package, assembly metadata, version tests, bridge manifest, PE version, download URL, and both changelogs agree on `2026.8.11-1`; final FTUE tests passed `22/22`.
- [x] Fresh Windows x64 MSVC `19.51.36252.0` Release builds and CTest `1/1` passed from new empty build directories at clean source commit `10a5ede95f2cbc73c1bc63d14b170b182a63a314`.
- [x] DLL is AMD64 native PE, imports only `KERNEL32.dll`, exports exactly the six V1 functions, embeds `2026.8.11-1`, and matches the installer-pinned byte length and SHA-256.
- [x] Two independent configure/build/package passes from the same clean commit produced identical DLL and ZIP hashes.
- [ ] Verified local install/importer/status/managed removal passed `1/1` against the packaged DLL on Unity `6000.4.12f1`; restart persistence, online release download, and clean-consumer package install remain pending.
- [x] Focused installer/security review has no remaining high or medium findings; the residual same-user destination-parent race is low and non-blocking because static reparse parents are rejected and trusted bytes remain held through import.
- [x] Final Setup Window tests passed `13/13` and localization/settings tests passed `23/23` after pinning the reproducible artifact.

### Unity matrix

- [ ] Unity `6000.4.12f1` compile, targeted suites, full EditMode, and PlayMode pass.
- [ ] Unity `6000.5.6f1` compile, targeted suites, full EditMode, and PlayMode pass.
- [ ] Unity `6000.6.0b7` compile, targeted suites, full EditMode, and PlayMode pass.
- [ ] Unity `6000.7.0a4` compile, targeted suites, full EditMode, and PlayMode pass.
- [ ] Matrix runs strictly one Unity process/version at a time; each process exits before the next starts.

### Attached RenderDoc matrix

- [ ] Pinned RenderDoc Direct3D 11 row passes with `Completed` / `Ready` / `Authoritative`, exact title/comments, replay XML, thumbnail, hashes, and the 2000 ms added-stall gate.
- [ ] Pinned RenderDoc Direct3D 12 row passes with the same gates.
- [ ] Pinned RenderDoc Vulkan row passes with the same gates.
- [ ] Sensitive `.rdc`, XML, thumbnails, and local matrix receipts remain outside Git and are not uploaded.

### Package publication

- [ ] `git diff --check`, MCP JSON parsing, `npm pack --dry-run --json`, and `npm publish --dry-run --access public --json` pass.
- [ ] Package contents contain no native binaries, RenderDoc binaries, sensitive captures, local logs, or credentials.
- [ ] Dry-run package SHA-1, integrity, file count, packed bytes, and unpacked bytes are recorded below.

## Boundaries

- Native support is limited to Windows x64 Unity Editor with Direct3D 11, Direct3D 12, or Vulkan.
- Development Player, Linux native, IL2CPP, mobile, macOS native, RenderDoc replay/analyzer, automatic upload, and PIX native integration are not claimed.
- The generic Unity `ExternalGPUProfiler` path remains available on its existing broader matrix but reports observed, non-authoritative artifacts.
- Public npm and Git UPM install pins remain on published `2026.8.9-1` until GitHub/npm publication, signatures, and provenance for `2026.8.11-1` are verified.

## Publication Gates

- [ ] Release commit/PR is merged to `main` and recorded.
- [ ] Immutable tag `2026.8.11-1` points to the exact release commit.
- [ ] Normal GitHub Release is published for the exact tag with the ZIP and `.sha256` assets.
- [ ] The published ZIP and extracted DLL download back with the package-pinned sizes and SHA-256 values.
- [ ] Owner-gated npm Trusted Publishing OIDC workflow succeeds.
- [ ] npm publishes `com.sungeargames.perfmeter@2026.8.11-1` and moves `latest`.
- [ ] Registry SHA-1/integrity, signature, attestation, and SLSA provenance v1 are verified and recorded.
- [ ] Public npm and Git UPM install pins advance only after publication verification.

## Publication Record

- Release source commit: `10a5ede95f2cbc73c1bc63d14b170b182a63a314`.
- Final release/tag commit: pending.
- Bridge DLL bytes/SHA-256: `125952`; `c86592e04e4e71a22e13cc8a462294082d14202e7112f6d3e90186d9ef88802b`.
- Bridge ZIP bytes/SHA-256: `234696`; `51adc728abb17eb157b8cff19186edf7c4a79341e85328cab98f978fdf013b13`.
- npm dry-run metadata: pending.
- Release PR, GitHub Release, workflow run, registry metadata, signatures, and provenance: pending.
