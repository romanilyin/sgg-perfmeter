# 2026.8.19-1 RenderDoc GPU Annotations Release

Status: release candidate.

## Scope

- Version/tag: `2026.8.19-1`.
- Package: `com.sungeargames.perfmeter`.
- Release work date: `2026-08-19`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- Native support row: Windows x64 Unity Editor with RenderDoc v1.46 on Direct3D 12.
- Optional artifact: `sgg-perfmeter-renderdoc-bridge-2026.8.19-1-windows-x64.zip` plus its `.sha256` file, distributed separately from the binary-free UPM package.

## Release Notes

- Added a RenderDoc-neutral public API for bounded typed GPU command annotations, ambient owner/generation context, and direct URP Render Graph command-buffer scopes.
- Added schema v1 machine keys, bounded typed values, explicit no-op statuses, non-nested scopes, and deterministic clearing after the annotated draw or dispatch.
- Extended the optional Windows x64 Editor bridge with an additive annotation ABI, generation-safe fixed packet pool, Unity native-plugin lifecycle, and initial Direct3D 12 command annotation transport.
- Preserved compatibility with the published capture-only bridge: capture remains available, while annotation calls report `BridgeTooOld` until the package-pinned `2026.8.19-1` artifact is installed.
- Fixed authoritative Embed export for RenderDoc's numbered capture filenames while preserving marker ownership and path-hardening checks.

## Validation

### Native artifact and installer

- [x] Package metadata, assembly metadata, installer manifest, PE version, artifact manifest, and both changelogs agree on `2026.8.19-1`; Unity version-contract tests remain part of the release test pass.
- [x] Two fresh Windows x64 MSVC `19.51.36252.0` Release builds and CTest `1/1` runs from clean source commit `c9e653f0f8a705b8c44c7cc0a313e2ee7ac6ecd8` produced identical DLL and ZIP hashes.
- [x] DLL is AMD64 native PE, imports only `KERNEL32.dll`, exposes the exact twelve capture/annotation/lifecycle exports, embeds `2026.8.19-1`, and matches the package-pinned size and SHA-256.
- [x] On Unity `6000.5.9f1`, local-file installation of the final packaged DLL, installed/ready status verification, managed removal, and post-removal `NotInstalled` status all pass; no DLL or importer metadata remains in the validation project.

### Unity and real capture

- [x] Primary Unity compile and targeted annotation, FTUE, setup, artifact-finalizer, storage, capture-bundle, lifecycle, and installer tests pass.
- [x] Isolated release-commit worktrees pass compile plus full D3D12 EditMode `549/549` with one opt-in replay smoke ignored and full PlayMode `20/20` on every supported installed row: `6000.4.12f1`, `6000.5.9f1`, `6000.6.0b7`, and `6000.7.0a4`.
- [x] A real RenderDoc v1.46 Direct3D 12 capture on Unity `6000.5.9f1` produced a `94,840,158`-byte authoritative embedded RDC (`6b31f51663ed00e9dbcf4f0fb567cde4169cd8e8cd746a5160d8644c65131c09`). Its `122,329,885`-byte XML proves all eight typed Sky raymarch keys are set before `ID3D12GraphicsCommandList::DrawInstanced` and deleted after it; bridge counters are packets `10/10`, annotation calls `80`, dropped `0`, errors `0`.
- [x] Clean package worktrees without the optional bridge compile on all four supported rows and preserve tested explicit no-op behavior.

### Package publication

- [x] `git diff --check`, `npm pack --dry-run --json`, and `npm publish --dry-run --access public --json` pass.
- [x] The dry-run package contains `373` entries (`1,030,461` packed bytes; `4,938,635` unpacked bytes); package-content and credential scans find no native binaries, RenderDoc binaries, captures, generated project state, local logs, or secrets.

## Boundaries

- Annotation transport is Windows x64 Unity Editor/Direct3D 12 only in this release and requires an already-loaded RenderDoc App API `1.7` instance plus an active capture.
- D3D11, Vulkan, Player, IL2CPP, Linux, mobile, Metal, nested scopes, and resource/object annotations require separate validation gates.
- The UPM package remains binary-free; neither the package nor bridge ships, loads, injects, or installs RenderDoc.
- Repository-facing npm and Git UPM install examples remain on published `2026.8.15-1` until GitHub/npm publication, provenance, and clean-consumer verification complete. The immutable release tag retains those prepublication package README pins by design.

## Publication Gates

- [ ] Release branch is merged to `main` and the immutable annotated tag `2026.8.19-1` points to the exact main release commit.
- [ ] A normal GitHub Release is published for the exact tag with the ZIP and `.sha256` assets.
- [ ] Published assets download back with the package-pinned hashes and the production installer completes online install/status/removal.
- [ ] npm Trusted Publishing OIDC publishes `com.sungeargames.perfmeter@2026.8.19-1` and moves `latest`.
- [ ] Registry integrity, signature, attestation, and SLSA provenance are verified.
- [ ] Clean npm-registry and tagged Git UPM consumers compile before public install pins advance.

## Publication Record

- Bridge artifact source commit: `c9e653f0f8a705b8c44c7cc0a313e2ee7ac6ecd8`.
- Final release/tag commit: pending.
- Bridge DLL bytes/SHA-256: `131072`; `cde023c8ec2f69969d58db1cb256acdd11631bfcd1af1b697c4d3b0fa3c17963`.
- Bridge ZIP bytes/SHA-256: `240338`; `119763028b054a2bfd141baf40bec171ba4313a4fbe6c52bee068a1efdd58299`.
- npm dry-run metadata: shasum `3ca844425b024c2e075db35ca425ccebd87c4fa8`; integrity `sha512-OLbypoQL0+y1cQXQ394ZJs6YvTaRxEWzI+jEY+jTUqL0Q2P/kQohNCVKbvfstBCYra1Qch+Bua839SHEH9phxg==`.
- Release merge, GitHub Release, workflow run, registry metadata, signatures, and provenance: pending.
