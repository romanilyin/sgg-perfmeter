# 2026.8.13-2 Overlay Corrections Release

Status: release candidate.

## Scope

- Version/tag: `2026.8.13-2`.
- Package: `com.sungeargames.perfmeter`.
- Release work date: `2026-08-13`.
- Runtime target: Unity `6000.4+` with URP `17.4+` Render Graph or HDRP `17.4+` Custom Pass integration.
- Fixes target-FPS budget persistence and the RAW strip severity palette without changing the RAW scale.
- Expands built-in visual preset width caps, simplifies the zero-code Default CPU-core composition, and persists `Presets > Save` as the active project/build preset.

## Release Notes

- The RAW frame-time strip retains its target-FPS budget through theme/font visual rebuilds and now uses dark green, blue, yellow, and red severity bands, with the former orange range merged into yellow.
- All built-in visual presets use the bounded `780 px` cap. Responsive wrapping remains active on narrow viewports, while custom preset widths are not migrated.
- CPU-core bars are disabled only in the zero-code Default configuration; Full Diagnostics and explicit module selection remain available.
- `Presets > Save` saves the descriptor, activates it in project Resources settings, and bakes all valid project descriptors for subsequent Play Mode sessions and builds without persisting unrelated unsaved controls or dropping unknown same-schema top-level fields.
- Recommended setup preserves valid settings and refuses to overwrite invalid or newer-schema JSON. Android smoke startup applies Resources settings and respects explicitly stopped configurations.

## Validation

- [x] Unity `6000.4.12f1` final targeted release suite passed `159/159`, including package-version and duplicate-preset-ID coverage.
- [x] Unity `6000.4.12f1` targeted overlay PlayMode passed `7/7`.
- [x] Unity `6000.4.12f1` full EditMode passed `534` with one opt-in RenderDoc replay smoke ignored out of `535` tests.
- [x] Unity `6000.4.12f1` full PlayMode passed `20/20`.
- [x] Focused implementation review found no remaining actionable correctness findings.
- [x] Release-version Unity `6000.4.12f1` compile passed; `PerfMeterFtueTests` passed `22/22` with package and assembly metadata on `2026.8.13-2`.
- [x] Release-version full EditMode passed `535` with one opt-in RenderDoc replay smoke ignored out of `536` tests; full PlayMode passed `20/20`.
- [x] `git diff --check`, `npm pack --dry-run --json`, and `npm publish --dry-run --access public --json` passed; publish dry-run does not validate OIDC authentication.
- [x] Package-content review covered `362` files, `1005709` packed bytes, and `4809692` unpacked bytes with no generated project state, native or RenderDoc binaries, captures, local logs, Python bytecode, or credentials.
- [x] Dry-run package SHA-1 is `3715a17f448d754af26ea6ce70754f16468ded24`; integrity is `sha512-5AjDSKWHvgH6D2T5wQ4vcXqnDvLDwBlQJWNwK4mCXVkhrECVONaJQak8H6jMRQ55PavAPZeFVe7u6jmZX0Do3A==`.
- [x] Android Vulkan Development build was attempted but explicitly waived: the installed Unity `6000.4.12f1` environment reports `Switching to AndroidPlayer is disabled`, so no APK or device-runtime result is claimed. The Android-only bootstrap change passed focused review, while shared settings behavior is covered by EditMode and PlayMode tests.

## Boundaries

- Validation is intentionally limited to the supported Unity `6000.4` line for this release; no additional `6000.5+` matrix is claimed.
- Runtime-tab and runtime API mutations remain in-memory changes for the current run. Project/build settings remain in `Assets/Resources/SGG.PerfMeter/perfmeter-settings.json`, while source descriptors remain under `Assets/SGG PerfMeter/Presets/Overlay`.
- Per-user Editor language and FTUE choices remain in `EditorPrefs`/Editor session state and are not included in project/build settings.
- The optional Windows x64 RenderDoc bridge remains the separately published `2026.8.11-1` artifact; no native artifact is published for this release.
- Repository-facing npm and Git UPM install examples remain on the already published `2026.8.13-1` until GitHub/npm publication, provenance, and clean-consumer verification complete. The immutable release tag will retain those prepublication pins by design.

## Publication Gates

- [ ] Release PR [#33](https://github.com/romanilyin/sgg-perfmeter/pull/33) merged to `main`.
- [ ] Immutable annotated tag `2026.8.13-2` points to the exact main merge commit.
- [ ] Normal GitHub Release is published for the exact tag.
- [ ] npm Trusted Publishing OIDC publishes `com.sungeargames.perfmeter@2026.8.13-2` and moves `latest`.
- [ ] Registry integrity, signature, attestation, and SLSA provenance are verified.
